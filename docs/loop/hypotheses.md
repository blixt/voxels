# Renderer Hypotheses

## 2026-03-12 browser benchmark harness search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The live `requestAnimationFrame` bootstrap loop is a trustworthy startup benchmark oracle in headless Chrome | Build the new browser benchmark harness on top of `getBootstrapBenchmark()` directly and run a cold-start smoke | Rejected. Headless automation needs a deterministic bootstrap drain path; relying on the live RAF loop kept the benchmark from finishing reliably enough to use as the harness baseline | Rejected |
| The startup benchmark can safely treat `chunkCount = 0` plus zero pending counters as “ready” | Run the first startup smoke and inspect whether the resulting CSV has meaningful chunk/sample counts | Rejected. The first pass went green in about `90 ms` with `chunkCount = 0`, `0` generated chunks, and no sample CSV. The kept gate now requires actual world entry plus zero pending/dirty/far backlog | Rejected |
| The internal bootstrap `completed` flag is the right browser-side acceptance gate for startup and for walk-benchmark setup | Compare it against the live snapshot state during the first headless startup and walk runs | Rejected. The internal flag stayed too strict for harness purposes because it waited on non-visual async meshing cleanup. The kept browser harness now uses a live world-state gate based on actual enterable/visually-ready conditions | Rejected |

## 2026-03-12 region-summary batching search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| Persisted per-column summary requests are still the right batching unit now that far rendering is driven by real chunk-derived data | Replace the async single-column summary request path with persisted region-summary requests, keep the browser smoke and resident-world regressions green, and compare the resulting architecture against the old path | Rejected. The single-column path worked as a stepping stone, but region batching is the cleaner long-term seam and let us delete the overlapping request/miss plumbing instead of keeping both | Rejected |
| Introducing region batching means we should keep the old column-summary request path around as a fallback | Wire region batching in parallel with the existing path and inspect whether both survive with a clear role | Rejected. Keeping both would only duplicate pending/miss bookkeeping and cache protocol complexity. The kept change removes the old async column path entirely while preserving chunk-derived column summaries inside each region payload | Rejected |
| A persisted region-summary batch still risks reopening the earlier far-field hole problems because summaries now arrive in larger bursts | Re-run focused resident-world tests plus a short Chrome route trace after the rewrite | Rejected. The tests stayed green and `region-summary-smoke` stayed at `0` hole-signal frames | Rejected |

## 2026-03-12 persistence-metric harness search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The new persisted summary counters can be trusted immediately because the existing cache-reuse benchmark already settles the world correctly | Run the fixed-origin cache-reuse probe after adding the counters and inspect whether the numbers are plausible | Rejected. The first run returned pathological zeros because `teleportAndSettle()` only advanced a single frame and the browser-ready gate allowed `chunkCount = 0` | Rejected |
| Fixing `teleportAndSettle()` and the ready gate is enough to make the cache-reuse benchmark trustworthy again | Re-run the same fixed-origin probe after the harness fix and compare the resulting chunk/summary hit counts | Confirmed. The rerun produced strong non-zero persisted summary hits outbound and strong persisted chunk hits on revisit | Confirmed |
| Persisted column-summary hits would already dominate the same-session cache-reuse path as soon as the metric existed | Inspect the fixed-origin cache-reuse probe after wiring the metric | Rejected. The counter is wired, but it stayed at `0` in this same-session scenario, which means the current path is still dominated by resident/frontier growth plus chunk-summary and chunk-payload reuse | Rejected |

## 2026-03-12 persisted column-summary search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| Chunk-derived render summaries are enough in memory; persisting per-column summaries separately is unnecessary duplication | Add a cache/worker path for persisted column summaries, keep them strictly derived from chunk summaries, and verify the browser runtime still behaves correctly after the protocol change | Rejected. The extra store gives the far-summary frontier a disk-backed seed without reintroducing generator coupling, and the browser smoke stayed clean | Rejected |
| Persisted column summaries should overwrite any current in-memory column view for the same `x/z` column | Wire persisted summaries directly into the live world regardless of whether actual chunk summaries for that column already exist | Rejected. In-memory chunk summaries from resident/edited chunks are the stronger truth within the current session, so persisted summaries are only used when the live world has no actual chunk-summary set for that column | Rejected |
| The current verification surface already proves whether persisted column-summary reuse is happening, so no further harness work is needed | Re-run the browser smoke after the protocol change and inspect whether it exposes cache-hit details for column summaries | Rejected. The smoke proves safety, not quantitative reuse. The next harness step should expose column-summary hit/miss counters explicitly | Rejected |

## 2026-03-12 far-summary frontier search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| Far-summary discovery still needs generator-backed Y-range probing even after chunk render summaries exist, otherwise the horizon cannot grow at all | Remove the generator fallback, grow prefetch only from resident/generated summary columns plus pending summary requests, and rerun the resident-world tests and a short Chrome route smoke | Rejected. The frontier now grows from actual summary data, the focused test proves `sampleColumn()` is not called by far-summary prefetch, and the route smoke stayed clean | Rejected |
| The old “far sampling becomes available after pre-generation” smoke already described the right contract | Re-run it under the summary-frontier model without changing the test | Rejected. The old test was still assuming generator-bootstrapped far discovery. The kept contract now requires the resident bubble to exist first, which is the correct architecture | Rejected |
| Once generator-backed far-summary discovery is removed, the remaining `sampleColumn()` hotspot in the route trace should disappear entirely | Rerun a short Chrome route trace and inspect whether any `sampleColumn()` inclusive cost remains | Rejected. `sampleColumn()` still appears via `computeChunkYRange()` for actual near-chunk generation, which is acceptable; the far-summary leak itself is gone | Rejected |

## 2026-03-12 far-column summary search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| Reconstructing a far-rendered `x/z` column by scanning every known chunk summary slab in that column is still cheap enough, so a dedicated aggregated column summary would just be extra state | Add a derived `GeneratedRenderColumnSummary`, switch `sampleFarFieldColumn()` to it, and compare the route trace plus far-column behavior against the pre-change path | Rejected. The dedicated column summary is simpler at the call site, makes far-column sampling O(1), and the short route smoke stayed clean | Rejected |
| A single aggregated column summary should track both “known vertical extent” and “non-empty vertical extent” so the far-prefetcher can distinguish empty-known slabs from unknown slabs | Build a pure helper, feed it mixed empty and non-empty chunk summaries, and lock the result in tests | Confirmed. The helper now tracks `min/maxKnownCy` and `min/maxNonEmptyCy`, and the regression test covers the distinction directly | Confirmed |
| Once the runtime has a per-column summary, generator-driven Y-range probing is no longer a meaningful architectural leak | Re-run a fresh route trace and inspect whether the old generator-backed far-summary range path still appears in the trace | Rejected. The new column summary helps once a column exists, but the trace still shows `computeFarFieldSummaryChunkYRange()` doing generator-backed work for unseen columns | Rejected |

## 2026-03-12 underground discovery and cave-opening search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| Underground discovery should require actual underground play context instead of just sampling the column metadata under the player | Add a pure helper that checks eye depth below the local surface plus overhead collision cover, then unit-test surface and cave eye positions | Confirmed. Surface walking no longer unlocks underground families, while realistic cave eye positions still do | Confirmed |
| Retuning the old shallow cave band is enough to create visible overworld entrances | Measure near-surface air and exposed opening counts after only shifting the upper cave band shallower | Rejected. The generator produced deep voids but still no real cave mouths | Rejected |
| Sampling neighboring terrain surface heights per column is the cleanest way to align cave mouths with slopes | Add a temporary local-relief probe using neighboring surface-height evaluation and compare opening counts plus generator-heavy test runtime | Rejected. It produced openings, but the extra work was expensive enough to push unrelated generator tests into timeouts | Rejected |
| A dedicated near-surface entrance band plus direct surface-breach support can create discoverable overworld cave openings without the expensive neighbor-surface path | Add an explicit entrance layer, count rugged-vs-flat cave-mouth ratios, and re-run the cave regression tests | Confirmed. The kept entrance layer produces real openings and passes the cave-mouth tests | Confirmed |
| Weight-curve tuning alone is enough to keep biome-specific cave mouths away from direct biome boundaries | Compare interior-vs-boundary opening ratios while only tuning biome-weight curves | Rejected. The ratios stayed inverted or collapsed to zero depending on the curve, so the approach was too brittle | Rejected |
| The generator needs an explicit nearby-biome suppression check for cave entrances, not just a generic biome-weight heuristic | Reuse biome classification in a cheap helper, query neighboring biome IDs, and damp entrance strength when direct neighbors disagree | Confirmed. Interior rugged-biome opening ratio now exceeds boundary ratio while keeping rugged caves much richer than flat-biome caves | Confirmed |

## 2026-03-10 visual error search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| `captureImage()` differs from the visible WebGPU canvas | Run `validationBlocks` in Chrome 146, inspect the live viewport, and compare it against the benchmark artifact after a cache-bypass reload | The live viewport and validation artifact agree once the page is running the current bundle | Rejected |
| Matrix layout or handedness is wrong | Project single points and single-voxel faces with `buildCameraMatrices()` and inspect projected bounds/orientation | CPU projection matched the software reference; row-major interpretation was obviously wrong | Rejected |
| Orthographic depth mapping is inverted | Compare clip-space depth for points stepped toward and away from the camera | Nearer points produce smaller depth values, matching `depthCompare: "less"` | Rejected |
| Face winding / culling is the main issue | Measure projected winding signs for single-voxel faces and render a custom cube through the current WebGPU depth/cull state | Winding was worth checking, but custom cube probes rendered correctly and did not reproduce the displaced frame | Rejected |
| Greedy mesher is emitting bad positions in the current source | Build the same single-voxel mesh under Bun and inside the browser page | Bun emitted `[1,1,1]..[2,2,2]`, while the cached browser page initially emitted `[1,1,1]..[10,10,10]` because it was still running an older bundle | Rejected for current source |
| A stale cached client bundle is serving pre-fix code | Compare browser-side single-voxel mesh bounds before and after `ignoreCache` reload, then rerun `validationBlocks` | Confirmed. After the no-cache reload, the browser emitted correct single-voxel bounds and `validationBlocks` passed in Chrome 146 | Confirmed root cause |
| Readback format or BGRA/RGBA swizzle is wrong | Render tiny offscreen primitives in `rgba8unorm` and inspect clear/background pixels | Minimal offscreen triangle and quad probes read back the expected bounds and colors | Rejected |
| Face-mask and quad object allocation are still a major meshing cost on the target runtime | Replace face-state objects with a packed integer mask, flatten quad records, and re-run the performance scenes in a fresh isolated Chrome 146 page with the new first-frame/warm-frame metrics | Confirmed on Chrome 146. Relative to the prior recorded browser baseline, mesh times fell to `terrain256 168.1 ms`, `editStorm256 173.0 ms`, `stressDrawCalls512 1.4 ms`, `stressMicroCubes256 155.4 ms`, and `stressScreens256 132.5 ms` | Confirmed |
| Terrain generation is wasting time doing one `setVoxel()` per vertical block | Add a chunk-aware `fillColumn()` path, rewrite terrain columns to use it, and compare current build timing in Bun and Chrome against the earlier recorded terrain/edit-storm baselines | Confirmed. Current build timings are down to `terrain256 36.3 ms` and `editStorm256 39.5 ms` in the warmed local Bun profiler, and `terrain256 53.6 ms` / `editStorm256 52.9 ms` in clean isolated Chrome 146 runs | Confirmed |
| One-shot Bun microbenches are a safe accept/reject oracle for renderer hot-path changes | Compare cold local probes, warmed local Bun profiles, and fresh isolated Chrome 146 reruns for the same mesher changes | Rejected. Bun/JSC is still valuable for fast local iteration, but keep/revert decisions for the engine now use Chrome 146 runs as the source of truth | Rejected as sole oracle |

## Locked-in regressions

- `tests/mesher.test.ts` now asserts that a single voxel only emits vertices inside `[1,1,1]..[2,2,2]`.
- `tests/camera.test.ts` now asserts that nearer points map to smaller clip-space depth values.
- Validation scenes now include tiny primitive cases before larger correctness scenes.
- The Bun server now serves HTML, CSS, and bundle responses with `Cache-Control: no-store` and cache-busted module URLs so browser-side probes exercise the current code by default.

## 2026-03-11 streaming performance search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The procedural game path is still meshing-bound first | Add `mise run profile-stream`, compare residency time vs mesh time for bootstrap `r3`, widen `r2 -> r3`, and shrink `r3 -> r2` | Rejected. Baseline local timings showed `bootstrap` stream `3673 ms` vs mesh `188 ms`, `widen` stream `2113 ms` vs mesh `147 ms`, and `shrink` stream `307 ms` vs mesh `58 ms` | Rejected |
| `generateChunk()` is wasting most of its time recomputing `sampleColumn()` and biome data per voxel | Cache per-column context once per chunk, reuse it for every voxel in that column, then rerun the same `profile-stream` cases and the Chrome game probes | Confirmed. Local stream timings dropped to `bootstrap 202 ms`, `widen 112 ms`, and `shrink 14 ms`; Chrome 146 `/` dropped to `bootstrap 206.5 ms`, `widen 104.6 ms`, and `shrink 13.0 ms` while resident-set counts stayed correct | Confirmed |
| Recomputing chunk solid bounds after generation is a meaningful avoidable cost | Carry chunk-local solid bounds out of `generateChunk()` and initialize resident chunks from them instead of rescanning chunk data | Confirmed as part of the same rewrite. The change is cheap, correct, and removes a full second pass over every streamed chunk | Confirmed |
| Duplicate neighbor-dirty marking is the next meaningful streaming bottleneck | Add residency phase metrics and dirty-chunk counts, then compare `neighborDirtyMs`, `dirtyResidentChunks`, and `generatedChunks` on `bootstrap`, `widen`, and `shrink` stream-profiler runs | Rejected as the next immediate target. `neighborDirtyMs` stayed below `1 ms`, while widen showed `179` dirty resident chunks for `115` generated chunks and shrink showed `64` dirty resident chunks for `115` evictions. The extra remesh cost is mostly real boundary work, not duplicate dirty bookkeeping | Rejected for now |
| Re-checking known-empty chunks is now a measurable part of shrink/update residency cost | Compare `generatedChunks` against `chunkGenerationMs` after the generator rewrite on `shrink r3 -> r2`, then verify the same pattern through `teleportAndSettle()` in Chrome | Confirmed. Shrink now reports `generatedChunks = 0` while still spending about `12-14 ms` in `chunkGenerationMs`, which means the cost is repeated evaluation of empty chunk positions rather than adoption of new solid chunks | Confirmed |
| Caching known-empty chunk coordinates will remove the remaining shrink/refresh generation cost without changing resident-set correctness | Store empty chunk keys after the first miss, rerun `profile-stream`, and verify `forceResidencyUpdate()` plus `teleportAndSettle()` in Chrome 146 | Confirmed. Warmed local `shrink r3 -> r2` dropped from about `14 ms` to `0.2 ms` with `26` cached empty hits and `0 ms` chunk generation. In Chrome 146, same-anchor refresh now hits `58` cached empties in `0.2 ms`, shrink hits `26` cached empties with `0 ms` generation, and widen reuses `58` cached empty results while still paying for `115` real chunk generations | Confirmed |
| Face-aware neighbor invalidation will materially reduce remesh work on stream widen/shrink | Count the unique resident neighbors that generated/evicted chunks would touch if invalidation only fired on actually touched faces, then compare that set against the current dirty/remesh counts | Rejected for the current terrain profile. At the default spawn, widen `r2 -> r3` still touches the same `179` resident neighbors under face-aware accounting, and shrink still touches the same `64` boundary neighbors | Rejected for now |
| Fully occluded solid chunks are wasting meshing time in the streamed procedural world | Count fully solid chunks with all six resident neighbors and verify whether that candidate is common enough, safe enough, and browser-verified enough to justify more mesher complexity right now | Inconclusive for now. The default streamed world does contain fully buried solid chunks, but the candidate was not kept because the browser-side proof loop was not strong enough yet relative to the larger scale/collision/streaming problems exposed by hands-on feedback | Parked |
| Draw distance can be fixed near-term with chunk-size or radius constants alone | Sweep radius and chunk-size values against the current generator + resident-world path and compare physical view distance against synchronous stream/mesh cost | Rejected. Radius `6` still only reaches about `192 cm`, while naïvely moving from `32^3` to `64^3` or `128^3` chunks explodes synchronous generation and meshing cost | Rejected |
| Single-chunk player movement is already cheap enough, and the visible hitch is mainly first-frame GPU upload | Add game-path boundary-cross probes that separate residency, meshing, sync, and upload cost, then compare `chunkDelta = 1` against `chunkDelta = 2` in fresh Chrome | Rejected. Fresh Chrome 146 `benchmarkChunkCrossing(2, 1)` still showed `changedCount = 4/4` with about `48 ms` stream and `195 ms` mesh on average, while upload was only about `0.73 ms`; the immediate problem is that one-chunk crossings still force full residency work at all | Rejected |
| A one-chunk stream-anchor hysteresis margin will suppress needless single-chunk churn without hiding the need for larger updates | Add a pure chunk-anchor helper with tests, wire it into the game controller, and compare `benchmarkChunkCrossing(2, 1)` against `benchmarkChunkCrossing(2, 2)` in fresh Chrome | Confirmed. After the hysteresis change, fresh Chrome 146 reported `changedCount = 0/4` for one-chunk crossings with `0 ms` stream and `0 ms` mesh, while two-chunk crossings still reported `changedCount = 4/4` with about `81 ms` stream and `180 ms` mesh | Confirmed |
| Taming the first world only needs parameter scaling, not a rewrite of the terrain model | Replace biome-owned height envelopes with a shared sea-level-centered terrain formula, keep biome influence small, and add deterministic envelope/biome-edge tests | Confirmed. The retuned generator now samples about `1424.6` average surface height around `seaLevel = 1400`, `25.4%` underwater coverage, `30` max adjacent step, and `30` max biome-edge jump across the fixed probe grids | Confirmed |
| String chunk keys or spawn search are the dominant startup problem | Compare hotspot timings before touching those paths | Rejected for now. The huge win came without touching string keys or spawn search, so those are not the main target at the current scale | Rejected for now |
| The far-field mesh cache stays correct when the near clear radius changes without crossing a far-field anchor | Rebuild `updateAround()` at the same world position with a larger clear radius and compare the nearest-band triangle count before/after | Rejected. The first implementation reused stale meshes because it only keyed rebuilds on anchor movement. The fix now keys reuse on both anchor and effective clear radius, and a direct test proves the nearest band rebuilds and sheds triangles in place | Rejected |
| Budgeted resident-world generation and budgeted meshing can preserve correctness while reducing game-path burst work | Re-run residency updates on the same anchor with a tiny generation budget and verify that they eventually converge to `complete = true`, then verify the next refresh becomes a no-op | Confirmed. Repeated `maxGenerateChunks = 4` updates now converge deterministically, keep pending work explicit, and stop redoing work once the anchor is complete | Confirmed |
| Once budgeted residency exists, it is enough to refresh the world only on player movement or anchor changes | Simulate a same-anchor partial update, then stop movement and verify whether pending chunks and dirty meshes keep draining | Rejected. The first controller path stalled same-anchor work after the initial partial update. The fix now keeps pumping while `pendingChunks > 0` or dirty resident meshes remain | Rejected |
| The old full-refresh stream profiler is sufficient for tuning the new game path | Compare tuning decisions from the full-refresh stream profiler against a new incremental crossing simulator that respects hysteresis and per-frame budgets | Rejected. The full-refresh profiler still measures useful worst-case bulk work, but it overstates perceived hitch once `/` uses bounded generation and meshing per frame | Rejected |
| A larger nearby resident ring is still viable if per-frame budgets are tuned around the incremental loop rather than one-shot work | Sweep `radius` and `generate/mesh` budgets with an incremental chunk-crossing profiler and compare the worst local frame against nearby-detail gain | Confirmed. `radius = 5` with `6/6` budgets keeps the local worst frame around `13.1 ms` for `chunkDelta = 2`, while preserving a materially larger near-detail ring than the older `radius = 3` baseline | Confirmed |
| Bootstrap, teleports, and forced residency refreshes can safely reuse the same budgeted path as ordinary player movement | Fresh Chrome reload on `/`, inspect grounded state and resident chunk count before interaction, then run a scripted teleport/settle probe | Rejected. The budgeted path allowed boot to accept a frame before the support chunk under the player was loaded, which dropped the player below the world. Force/bootstrap paths now run a full settle pass again while ordinary movement stays budgeted | Rejected |
| Prioritizing center/support chunks first will make the budgeted path degrade gracefully instead of loading arbitrary far columns first | Add a unit test with `maxGenerateChunks = 1` at spawn and assert that the support chunk under the player becomes resident first | Confirmed. Distance-sorted column order plus surface-centered Y priority now loads the spawn support chunk on the first budgeted update | Confirmed |
| The existing `benchmarkChunkCrossing()` harness is still a faithful proxy for ordinary movement hitch after restoring full-settle teleports | Compare what the function does internally against the post-fix browser numbers | Rejected. It now intentionally measures correctness-oriented settle work because `teleportAndSettle()` fully resolves residency and meshing. Keep it for correctness and add a separate incremental-movement probe for real hitch measurements | Rejected |
| Bigger per-frame generation/meshing budgets will necessarily improve the game path because they clear the backlog faster | Add a separate incremental-crossing benchmark and sweep budget pairs in Chrome while comparing combined work, pending backlog, and completion frames | Rejected. Higher budgets did reduce backlog, but they created much worse combined spikes once stream and mesh work landed together in the same frame | Rejected |
| The old `12/12` budget defaults are still the best compromise after adding a real movement-path harness | Compare `12/12` against lower-budget candidates under the new incremental benchmark in fresh Chrome 146 | Rejected. `12/12` was much burstier than the low-budget candidates, with combined p95 work in the mid-20 ms range instead of the low teens | Rejected |
| A lower-budget regime around `generate = 6`, `mesh = 4` can smooth movement hitch without stalling the world indefinitely | Verify the pair in both the local game-stream profiler and the browser incremental-crossing benchmark | Confirmed. The pair produced about `11.7 ms` max frame work locally on `crossing-d2` and about `12.7 ms` p95 combined work in Chrome while keeping progress continuous | Confirmed |
| Recomputing per-column chunk Y ranges is the next dominant stream-side cost after the budget search | Extend the local game-stream profiler with stream phase totals and compare `yRangeMs` against `chunkGenerationMs` on `crossing-d2` | Rejected. The richer profiler showed only about `2.8 ms` in total Y-range work versus about `88.9 ms` of real chunk generation, so caching the Y range is not worth keeping as a complexity increase | Rejected |

## 2026-03-11 far-field LOD seam correctness search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The visible LOD holes are just a perception issue caused by fog/lighting rather than actual coverage bugs | Add a runtime browser probe that samples around the player and counts resident/far overlap, gaps, band overlap, and wrong-band coverage | Rejected. The probe shape was worth keeping, but the user report was real and pointed at concrete geometry/coverage faults instead of only shading | Rejected |
| The far-field mask is aligned with the true streamed footprint, so overlap must be coming from the renderer | Compare far-field coverage against actual resident chunk columns instead of a generic near clear radius, then re-run the browser overlap probe | Rejected. The old game path masked against a broad clear radius instead of the real resident columns, which allowed both overlap and holes around the streamed footprint | Rejected |
| The coarse far-field shell already emits all needed side faces and the “backface” look is caused elsewhere | Render a tiny stepped-column case and assert the mesh contains normals for `+X`, `-X`, `+Z`, and `-Z` | Rejected. The old mesh path only emitted part of the vertical shell; the four-direction face test now locks this down | Rejected |
| Centering band coverage on a snapped far-field anchor is harmless as long as the anchor window is big enough | Hold the player inside one anchor window, then check whether the player position itself is incorrectly classified into a far-field band | Rejected. The old logic could classify cells near the player into coarse bands simply because the anchor lagged behind the player | Rejected |
| Re-centering band coverage around the player while keeping the grid anchored will restore correctness without forcing obvious movement-path regressions | Add a direct inner-hole test, then re-run browser probes after settled teleports and a budgeted two-chunk crossing | Confirmed. After the re-center, browser probes reported zero resident overlap, zero gaps, zero band overlap, and zero wrong-band samples in both settled and budgeted movement checks | Confirmed |
| Rebuilding far-field meshes before updating streamed residency is harmless because the next frame will catch up anyway | Move far-field updates to after residency changes and compare the overlap/gap probe on teleports and movement sweeps | Rejected. Rebuilding against the previous resident set leaves a one-update mismatch that is easy to catch with the coverage probe | Rejected |
| Once the resident seam is fixed, the broad far/horizon span is also guaranteed gap-free | Probe the full `224 m` span at coarse step size after settled teleports | Confirmed for resident overlap and uncovered gaps. The broad probe now reports zero resident overlap and zero gaps across the span | Confirmed |
| The same fixes also eliminate all inter-band overlap at the far/horizon handoff | Use the broad probe to count multi-band samples after the resident seam fix | Rejected for now. The obvious seam bug is fixed, but there is still measurable overlap at the exact far/horizon handoff around `224 m` | Parked |

## 2026-03-11 render-ready handoff and coarse-detail follow-up

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The remaining “hole before near chunk appears” bug is really a resident-vs-rendered handoff problem | Distinguish resident columns from render-ready columns, then probe for samples where the far field is gone before detailed meshes are ready | Confirmed. Resident columns were being excluded too early; switching to a render-ready mask removed the handoff-hole failure | Confirmed |
| Reordering the runtime to `residency -> mesh build -> far-field rebuild` will remove the temporary holes without hurting the movement benchmark | Change the order in the game/profile paths, then re-run the movement probe and incremental benchmark | Confirmed. `handoffHoleCount` dropped to `0` on a pending-stream step, and the incremental benchmark stayed in the low-teens | Confirmed |
| The first coarse band is still too coarse too close to the player, which makes the near/far border look abrupt even when it is technically gap-free | Add a nearer `transition` band and probe which sample stride is visible at `18 m`, `24 m`, `36 m`, `48 m`, and `60 m` | Confirmed. The visible coarse stride now stays at `0.8 m` out to `36 m` and only steps to `1.6 m` at `48 m` | Confirmed |
| Coarse cells are dropping small water features because they sample too little information per cell | Preserve water with extra sub-samples in the nearer coarse bands, add a direct unit test, and then find a water patch in the live browser far field | Confirmed. The unit test passes and a browser scan found a preserved water patch in the live `transition` band | Confirmed |
| The “see inside the shell” seam is caused by masked edges not emitting their closing wall when the detailed side is lower | Add a masked-lower-neighbor seam test and then re-run the browser seam probe | Confirmed. The seam-wall unit test passes, and the remaining measured issues moved away from the resident/far seam and into higher-level band compositing | Confirmed |
| Once the transition band is in place, exact-radius band-boundary artifacts disappear as well | Re-run the coverage probe after the transition-band change | Rejected for now. The obvious handoff holes are fixed, but exact-radius band-boundary overlap/gap artifacts still show up in the coverage probe | Parked |

## 2026-03-11 corrected movement-cost search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The remaining “near chunks are late” complaint is still mainly chunk generation or detailed meshing, and the coarse LOD path is already cheap enough | Monkey-patch `farField.updateAround()` in fresh Chrome around `benchmarkIncrementalCrossing(1, 2, 12, 20)` and compare the hidden far-field total against the benchmark summary | Rejected. The browser benchmark was hiding a far-field tax of about `5120.6 ms` total across `66` calls, about `77.6 ms` average, and about `192.8 ms` max, so the movement oracle itself was misleading | Rejected |
| The right next step is to micro-optimize chunk generation before touching the far-field update policy | Change the benchmark first so it counts far-field work, then compare that corrected total against stream and mesh cost | Rejected. Once corrected, the benchmark showed far-field rebuilds were still a major share of movement work, so tuning generation first would have been the wrong local optimum | Rejected |
| Clipmap-style center snapping can cut far-field rebuild churn without reintroducing the obvious near/far holes | Snap band centers on larger strides, add a unit test that the default bands stay stable across a few meters of movement, and re-run the browser monkey-patch probe | Confirmed. The stability test passes, and the same browser probe dropped from about `5120.6 ms` total far-field work to about `717.4 ms` | Confirmed |
| A per-frame far-field rebuild budget will let the runtime spread coarse-band work out without dropping already-built far meshes | Add `pendingBands`, keep old meshes when the budget is exhausted, and unit-test that deferred bands drain on the next update | Confirmed. The budgeted-band test passes, and the runtime can now defer excess band rebuilds while continuing to render the prior far mesh | Confirmed |
| Once far-field work is budgeted, the background pump logic can stop as soon as chunk generation and detailed meshing are done | Extend `shouldPumpWorldWork()` with far-field pending-band state and add a unit test | Rejected. Without far-field pending-band awareness, budgeted coarse-band work can stall forever after movement stops | Rejected |
| After far-field throttling, the next dominant problem is likely detailed near-chunk prioritization rather than another far-field seam issue | Re-run the corrected browser benchmark and compare `avgFarFieldMs` against backlog and incomplete-frame counts | Confirmed for next-step planning. Far-field churn dropped sharply, but the corrected benchmark still reports `maxPendingChunks = 114` and many incomplete frames, which points toward detailed-path prioritization as the next likely win | Confirmed |
| Sorting dirty chunk meshes around the player is too weak to matter; a real queue is required immediately | Prioritize unbuilt nearby chunks first, then compare corrected browser `benchmarkIncrementalCrossing()` numbers before/after | Rejected. The simple player-centered sort already moved corrected browser work from about `17.5 ms` average / `32.7 ms` p95 down to about `9.9 ms` average / `17.4 ms` p95 | Rejected |
| Prioritizing nearby detailed chunks will only change mesh timings and will not materially affect the corrected far-field numbers | Compare corrected browser `avgFarFieldMs` before/after the mesh-priority change | Rejected. `avgFarFieldMs` dropped from about `11.1 ms` to about `2.45 ms`, which suggests that getting nearby chunks render-ready earlier also reduces churn in the coarse handoff path | Rejected |
| Once nearby chunks are prioritized, we no longer need a special oracle for how “late” nearby detail is | Add a direct render-ready coverage probe around the player and sample it during the corrected movement benchmark | Rejected. The new oracle still shows `avgResidentNotReadyNearSamples = 223.8` and `maxResidentNotReadyNearSamples = 360`, so the game can remain materially behind even when the overall benchmark looks much better | Rejected |
| The next useful optimization target is still general total work, not specifically the area around the player | Compare corrected work totals against the new near-detail coverage counts | Rejected for next-step planning. Total work improved a lot, but the near-detail coverage probe makes it clear that the remaining pain is localized detailed readiness around the player, not just a generic frame-time average | Rejected |

## 2026-03-11 render-ready mask presentation and near-detail prioritization

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The remaining movement hitch is still mostly “real” far-field layout work rather than repeated presentation of render-ready mask churn | Hold the far field spatially up to date, but freeze the presented render-ready mask revision while detailed chunks are still pending or dirty; compare local `crossing-d2` totals against the immediate-presentation policy | Confirmed. Local `crossing-d2` far-field time dropped from roughly `958 ms` total to roughly `97 ms` total while the far field still tracked player movement spatially | Confirmed |
| Freezing the whole far field during backlog is the right runtime policy | Compare a full far-field freeze against a mask-only freeze and inspect whether the remaining expensive frame is a delayed anchor rebuild instead of continuous churn | Rejected. Freezing only the presented mask revision keeps the far field spatially current while still killing most of the churn, which is a cleaner compromise than freezing the whole far field | Rejected |
| Once the mask-presentation churn is reduced, the next practical win is to mesh nearby dirty chunks first instead of continuing in arbitrary resident iteration order | Add a tiny mesher test with three dirty chunks and a focus point, then run the stream profiler using that focus point as a meshing priority hint | Confirmed. The focused meshing test passes, and the game/profile paths can now spend limited mesh budget on nearby unbuilt chunks first | Confirmed |
| Raising the default near-stream budgets is still too risky even after the mask-presentation fix | Sweep `8/6` against the old `6/4` in the local game-stream profiler after the new far-field scheduling change | Rejected. `8/6` reduced settle-frame count enough to be worth keeping as the new default, while staying within the same broad hitch envelope as the old defaults | Rejected |

## 2026-03-11 wider near range and masked seam closure

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The remaining tiny “void” holes at the near/far border are only mask-level bookkeeping issues and not actual coarse-mesh geometry gaps | Add a dedicated masked seam probe that samples the detailed boundary surface against the generated far-field wall geometry | Rejected. The mask-level coverage probe was insufficient by itself; the remaining bug was real geometry on masked seam walls | Rejected |
| Masked seam walls are already long enough and the visible gap is just a camera/fog artifact | Build a flat masked seam case and inspect the generated wall extents directly in a unit test | Rejected. The old wall generation could stop too high when the masked detailed side was lower or absent, leaving a visible slit into the void | Rejected |
| Closing masked boundaries against the generator-sampled boundary minimum plus a small downward skirt will eliminate the visible seam hole without reopening overlap | Rebuild the masked seam wall logic, then rerun the new seam-gap probe in tests and in Chrome | Confirmed. The unit probe now reports zero seam gaps and the browser seam oracle reports `gapCount = 0` | Confirmed |
| The first coarse band is still too near the player even after the earlier transition-band work | Widen the resident ring and rebalance the default bands, then sample the visible coarse stride at fixed distances from the player | Confirmed. The first visible coarse coverage now begins materially farther away, with `0.4 m` coarse cells not showing up until roughly the high-20-meter range | Confirmed |
| The nearest transition band can keep its old `12.8 m` center stride without materially affecting movement-path cost | Re-run the incremental browser benchmark after widening the band layout but before changing the stride | Rejected. The tighter stride still caused too much near-transition churn; raising it to `25.6 m` materially reduced average far-field cost on the movement path | Rejected |
| Once masked seam walls are fixed, the remaining obvious user complaint is mostly sustained movement backlog rather than seam geometry | Compare the seam oracle after settling against the incremental movement benchmark under the widened near-range configuration | Confirmed. The seam oracle is clean, while the movement benchmark still shows backlog and incomplete detail readiness under continuous movement | Confirmed |

## 2026-03-11 deterministic route benchmark search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The old chunk-crossing probes already explain the real 10-second gameplay experience well enough | Replace them with a fixed-seed 10-second route at realistic run speed plus a settle phase, then compare whether it exposes anything new | Rejected. The new route benchmark exposed visible-ground gap frames and delayed settle behavior that the crossing probes did not characterize well | Rejected |
| There is still a large hidden CPU bucket in the gameplay path, so microbenchmarks are undercounting the real frame cost | Add explicit total gameplay-frame timing and compare it against accounted movement + stream + mesh + far-field + render CPU time | Rejected. On the fresh Chrome route run only about `1.16%` of gameplay-frame time remained unmeasured, so the benchmark now has good accounting coverage | Rejected |
| The current visible-hole issue is mainly just resident-but-not-ready detail immediately around the player | Compare the old near-radius render-ready probe against a new forward visible-ground coverage probe on the same route | Rejected. The route benchmark found many frames with forward visible-ground gaps even while `residentNotReadyNearSamples` stayed at `0` | Rejected |
| Screen-space hole capture is required before the benchmark can say anything useful about visible holes | Add a low-resolution lower-center viewport slit detector and compare it against the forward-ground probe on the route | Rejected for now as a primary oracle. The viewport capture stayed mostly quiet, while the forward-ground probe reliably caught the actual streaming-hole frames | Rejected |
| Far-field rebuilds are no longer the dominant gameplay hitch once the widened near range and seam fixes are in place | Inspect the worst route frames under the new total-accounting benchmark | Rejected. The worst frames are still dominated by single far-field spikes around `397 ms`, much larger than simultaneous stream or mesh work | Rejected |
| A deterministic route plus settle phase will make “seconds to recover after stopping” measurable enough to guide the next optimization round | Run the route benchmark with a bounded settle window and record the first clean settle frame | Confirmed. The current route benchmark still needs `52` settle frames after movement to fully drain under the tested configuration | Confirmed |

## 2026-03-11 clipmap-style far-field reuse

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The big far-field spikes are mostly whole-band terrain resampling on anchor shifts, so cache shifting should matter more than another parameter tweak | Add per-band far-field timing and sampled-cell metrics to the route benchmark, then inspect the worst route samples | Confirmed. The worst route frames were dominated by near-transition and transition sample-cache rebuilds, with `137,641` sampled cells and `~374 ms` sample-cache time on the largest spike | Confirmed |
| The current local `crossing-d2` stream profiler is already exercising the same far-field anchor-crossing path as the browser route benchmark | Compare far-field sampled-cell totals and band attribution between the local crossing profiler and the route benchmark | Rejected. `crossing-d2` never crossed a far-field anchor and reported `0` sampled cells, so it was missing the real spike class entirely | Rejected |
| Geometry-clipmap-style cache shifting can be applied directly to the current far-field representation without changing visible results | Shift the sample cache by whole sample-cell deltas on anchor movement, then compare the rebuilt mesh against a fresh far field at the destination anchor | Confirmed. The new unit test shows shifted-cache rebuilds resample fewer cells and still produce the same mesh checksum as a cold rebuild | Confirmed |
| Sample-cache shifting is too small a win to matter on the actual route benchmark because mesh build, not sampling, dominates everything | Re-run the 10-second browser route benchmark after the cache-shift change and compare the far-field spike envelope | Rejected. The route benchmark improved materially: max far-field time fell from about `419 ms` to about `94 ms`, and max gameplay-frame CPU fell from about `415 ms` to about `111 ms` | Rejected |
| Once sample-cache shifting is in place, far-field spikes are basically solved | Re-inspect the worst route samples after the cache-shift change | Rejected. The remaining worst no-resample near-transition mesh rebuilds still cost around `38-40 ms`, so mesh-build cost is now the next far-field target | Rejected |

## 2026-03-11 close-range fallback coverage

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The remaining ground holes are still a seam bug between detailed and coarse representations | Re-run the route benchmark and compare seam-gap counts against visible-ground gap counts | Rejected. Seam-gap counts stayed at `0` while visible-ground gap frames remained high, so the problem was not a seam wall bug anymore | Rejected |
| The route holes are mostly caused by the intentionally empty near-transition inner radius, not by late render-ready masking | Inspect the route-hole samples and check whether the uncovered cells are nonresident/non-render-ready only a few meters ahead of the player | Confirmed. The uncovered samples clustered only a few meters ahead in columns that were neither resident nor render-ready, which matches the inner-hole policy rather than a render-ready seam error | Confirmed |
| Letting the near-transition fallback cover all the way to the player will fix the hole without materially harming the current frame-time envelope | Remove the forced `6 m` near-transition inner radius, rerun the deterministic route benchmark, and compare visible-ground gap counts plus frame-time summary | Confirmed. The route benchmark dropped from `64` visible-ground gap frames to `0`, while gameplay and far-field timing stayed essentially flat | Confirmed |

## 2026-03-11 settled-reference diff and retained detailed meshes

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The user-visible remaining hole is still adequately captured by the forward-ground probe, so no stronger visual oracle is necessary | Capture sampled route frames during movement, replay the same poses after settle, and compare transient-vs-settled images for `clear -> filled` terrain pixels | Rejected. The stronger benchmark still found a real transient hole frame even when `visibleGroundUncoveredCount = 0`, so the earlier proxy alone was insufficient | Rejected |
| The remaining disappearing-geometry bug is mostly a far-field policy issue rather than a detailed-mesh lifecycle issue | Inspect a suspicious route frame together with the code path for resident chunk dirtying and compare against chunk lifecycle counters | Rejected. The stronger sample lined up with many dirty resident chunks, and code audit showed adjacent chunks were clearing their mesh immediately when dirtied | Rejected |
| Keeping an existing detailed mesh visible until its remesh replacement is ready will remove the transient holes without needing another far-field hack | Stop nulling neighbor meshes on dirtying, rerun the same-pose settled-reference route benchmark, and compare hole-signal counts | Confirmed. `framesWithSettledReferenceHoleSignals` dropped from `1` to `0`, while the benchmark still exercised heavy streaming backlog | Confirmed |
| The earlier `near-transition.innerRadius = 0` policy is still necessary after the retained-mesh fix | Restore the original `6 m` inner radius and rerun the stronger same-pose settled-reference route benchmark | Rejected. The route benchmark stayed clean after restoring the original inner radius, so the retained-mesh fix made the speculative far-field tweak unnecessary | Rejected |
| The current default `8/6/1` stream budgets are still the best gameplay tradeoff after the retained-mesh fix | Run a small deterministic route sweep over nearby generation/mesh budgets and compare average/p95 gameplay frame time plus hole signals | Rejected. `7/6/1` produced a better deterministic route balance than `8/6/1`, with lower average and lower `p95` while keeping the stronger hole oracle clean | Rejected |

## 2026-03-11 downhill seams and live movement far-field spikes

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The remaining tiny far-field slits on downhill slopes are only masked-seam artifacts and not rendered-neighbor downhill boundaries | Add a new far-field surface-gap probe that also checks rendered downhill boundaries against generator-sampled boundary minima | Rejected. The stronger probe needed to cover both masked seams and downhill rendered neighbors to match the reported artifact class | Rejected |
| Extending downhill side walls to the sampled boundary minimum will remove the remaining tiny far-field slits without reopening overlap | Cache sampled boundary minima, use them in wall-bottom selection, and rerun the new surface-gap probe in tests and Chrome | Confirmed. The downhill surface-gap probe now reports `gapCount = 0` in the synthetic test case and in the live browser world | Confirmed |
| The remaining `Far Build` spikes while walking are still real far-field work on move frames, not just mixed settle-phase reporting in the summary | Inspect raw per-frame route samples and then run a live held-`W` benchmark on the actual interactive loop | Partially confirmed. The route summary was mixing move and settle phases, but the live interactive loop still showed real far-field spikes under held movement input | Confirmed |
| Using `moved` as the live scheduler signal is good enough to distinguish active walking from settle frames | Hold movement input in the live loop while the player briefly stalls against terrain and watch whether far-field work is still allowed to run | Rejected. Brief stalls with movement input still held were enough to trigger far-field rebuilds, which matches the user-visible hitch | Rejected |
| Suppressing far-field rebuilds based on movement intent instead of actual displacement will remove the live walking far-field spikes | Change the live loop to key off movement intent, then rerun the held-`W` benchmark and inspect `maxFarFieldMs` | Confirmed. The held-`W` benchmark dropped from about `18.3 ms` max far-field work to about `0.2 ms` while input remained active | Confirmed |
| Current far-field rebuild frequency is already honoring the per-band `centerStride` settings | Add a unit test with different near/far `centerStride` values and move only far enough to cross the near stride | Rejected. The implementation was snapping all bands to the smallest center stride, so far bands were rebuilding too often | Rejected |
| Honoring each band's own `centerStride` is a worthwhile low-risk optimization for walking cost | Try the change and rerun the settled LOD coverage oracle | Rejected. The change reduced rebuild churn but created real uncovered settled far-field gaps, so it was reverted | Rejected |
| A smaller generation/mesh budget is the next obvious fix for the remaining walking spikes | Run short live held-`W` budget sweeps and compare max combined work plus visible-ground coverage | Rejected for now. The sampled sweep did not produce a strong enough improvement to justify changing defaults yet | Rejected |

## 2026-03-11 runtime far-field masking regression search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| A simple cadence rule for far-field catch-up while moving will by itself remove the lingering overlap and CPU complaint | Add a scheduler helper, test cadence values, and compare held-walk pending-band drain against top far-field work | Rejected as a full fix. The cadence helper is useful as a seam, but scheduler tweaks alone did not explain the severe CPU regression or the lingering overlap/hole behavior | Rejected |
| Building a runtime far-field mask on the CPU is cheap enough even if it queries `hasRenderReadyColumn()` for each cell | Inspect the helper cost and audit what `hasRenderReadyColumn()` does internally | Rejected. The helper rebuilt the full render-ready column set for every queried cell, creating a catastrophic per-frame CPU bomb | Rejected |
| The clean fix is to keep rebuilding coarse far-field meshes whenever the render-ready footprint changes | Compare the policy cost against a path that keeps geometry stable and masks overlap at render time instead | Rejected. Rebuilding far-field geometry just to match the transient detailed footprint creates unnecessary churn and keeps overlap correctness tied to mesh rebuild timing | Rejected |
| Rendering far-field meshes through a GPU-side render-ready column mask can preserve correctness without paying repeated coarse rebuild cost | Build far meshes without dynamic exclusion, send a compact cached chunk-column bitset to the GPU, and discard far fragments that land in render-ready columns | Confirmed by code and automated validation. The runtime path is leaner, the catastrophic CPU helper was removed, and the next step is fresh browser tracing on this new baseline | Confirmed |

## 2026-03-11 real Chrome trace search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The route benchmark numbers alone are enough, so there is no need to inspect complete Chrome traces | Capture full production/dev Chrome traces for the deterministic route and parse CPU/heap/GC directly | Rejected. The traces added real attribution that the benchmark summaries do not expose, especially around heap blow-ups and live-loop overhead | Rejected |
| A production trace alone is enough to guide the next fix | Pair a production trace with a dev trace that preserves readable function names | Rejected. Production was necessary for timing, but the dev trace was necessary to attribute cost to concrete functions like `buildChunkMesh()`, `generateChunk()`, `buildBandMesh()`, and `pushHud()` | Rejected |
| The deterministic route harness is already a perfect proxy for the actual gameplay loop | Capture a second complete Chrome trace on the real live loop during held movement, then compare hot frames against the route trace | Rejected. The route trace was still valuable, but the live-walk trace exposed always-on HUD and renderer-sync costs that are easy to underweight if only the benchmark path is traced | Rejected |
| The remaining allocation problem is broad background churn rather than a specific subsystem | Correlate the largest heap rise/drop windows in the dev route trace against sampled CPU frames in those windows | Rejected. The worst heap event lines up with far-field `buildBandMesh()` and especially `pushQuad()` / `buildMeshData()` / `writeVertex()` | Rejected |
| HUD rendering is too small to care about because it is “just debug UI” | Capture a live-walk trace and inspect exclusive CPU time on the actual gameplay loop | Rejected. `set innerHTML` and `pushHud()` are large enough on the live loop to deserve a real fix | Rejected |
| Renderer resource sync is negligible once upload counts are low | Inspect the live-walk trace rather than only the benchmark summaries | Rejected. `syncResources()` and repeated resident-chunk iteration remain measurable even when frame-local uploads are small | Rejected |

## 2026-03-11 trace-backed cleanup follow-up

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| Replacing the HUD `innerHTML` rebuild with persistent nodes will remove a real live-loop CPU hog rather than produce noise-level gains | Rewrite the HUD to build metric nodes once, then compare a fresh live-walk trace against the previous one | Confirmed. `set innerHTML` disappeared from the live-walk hot-frame list and HUD work is no longer a dominant exclusive bucket | Confirmed |
| The far-field heap blow-up is fundamentally caused by transient JS `number[]` mesh staging arrays, so a typed builder rewrite should materially reduce heap pressure | Replace the far-field `number[]` mesh staging path with direct typed writes, then compare fresh route/live traces against the prior traces | Confirmed. Heap max dropped from `168.43 MB -> 19.56 MB` on the dev route trace and from `67.63 MB -> 18.20 MB` on the live-walk trace | Confirmed |
| The earlier heap diagnosis is still too manual to use efficiently in future rounds | Extend the trace analyzer to report the hottest sampled frames inside the biggest heap-rise/drop windows, then run it on the fresh traces | Confirmed. The analyzer now emits direct heap-window hot frames, which again point to far-field band construction plus generator sampling rather than an unknown background allocator | Confirmed |
| Once the HUD and transient far-field arrays are fixed, renderer-side overhead will stop mattering relative to world generation and meshing | Re-run the live-walk trace and inspect the remaining top exclusive frames | Rejected. `syncResources()` still shows up as a clear renderer-side always-on cost, even after the HUD fix and far-field allocator rewrite | Rejected |

## 2026-03-11 renderer sync reuse search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The current renderer sync path is paying too much fixed CPU for full resident-set bookkeeping and exact-size GPU buffer churn | Re-read the current `syncResources()` / upload path and compare it to the live-walk trace attribution | Confirmed. The old path rebuilt a `Set`, walked the resident world twice, and recreated buffers for dirty meshes even when the owner stayed resident | Confirmed |
| Reusing GPU buffers in place when capacity is sufficient will materially cut renderer-side live-loop cost without needing a broader architectural rewrite | Add reusable buffer capacities and re-run the route/live traces | Confirmed. Live exclusive `syncResources()` dropped from about `78.8 ms` to about `52.8 ms`, and production route max frame time improved from about `61.4 ms` to about `53.8 ms` | Confirmed |
| Persistent uniform and far-field-mask staging buffers are too small to matter | Fold them into the same renderer pass and compare the fresh traces rather than dismissing them | Confirmed as worth keeping. They are not the largest gain alone, but they remove avoidable per-frame allocations from the renderer hot path | Confirmed |
| Once renderer-side sync reuse lands, renderer overhead stops being the next meaningful target | Re-run the live-walk trace and inspect the new top exclusive buckets | Rejected. Renderer sync improved, but detailed chunk meshing is now the clearer next hotspot and renderer sync helpers still remain visible | Rejected |

## 2026-03-11 mesher scratch reuse search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| After the renderer pass, the next practical gain is still in transient mesher scratch churn rather than immediately rewriting the meshing algorithm | Re-run the live-walk trace and inspect the biggest heap-rise window before changing anything | Confirmed. The biggest rise window had shifted onto `buildChunkMesh()` plus generation work | Confirmed |
| Reusing greedy-mesher `mask` and `quads` scratch storage will be a measurable win even without changing the actual greedy algorithm | Add a bounded scratch pool, re-run the route benchmark and live-walk trace, and compare `avgMeshMs` plus `buildChunkMesh()` hot-frame time | Confirmed. `avgMeshMs` improved from about `3.02 -> 2.78`, and live exclusive `buildChunkMesh()` improved from about `470.8 ms -> 459.8 ms` | Confirmed |
| The old dirty-chunk collection path is already cheap enough that gathering the full resident set and then filtering it is not worth touching | Replace the spread/filter path with direct dirty collection and watch whether the trace or benchmark worsens | Rejected. The direct collection path is simpler and cheaper, and it contributed to the same mesher cleanup slice without any regression | Rejected |
| Mesher scratch reuse alone is enough to remove chunk meshing from the top of the live trace | Re-run the live-walk trace after the scratch reuse pass | Rejected. The slice is worth keeping, but meshing and generation still clearly dominate the live loop | Rejected |

## 2026-03-11 procedural generator hot-path search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| A direct-mapped `valueNoise2D()` cell cache is the next clean win because adjacent samples should hit the same noise cells repeatedly | Add a tiny bounded cache, then compare route summaries and live-trace hotspots against the no-cache noise cleanup | Rejected. The cache introduced a new `valueNoiseCellCacheIndex()` hotspot and did not produce a strong enough real-world win to justify the added complexity | Rejected |
| The simpler noise cleanup is still worth keeping even without the cache | Keep the `Math.imul(...)` noise math, inline interpolation, cached seeds, and reciprocal scales, then run focused tests plus a fresh build | Confirmed. The simpler path stays correct and reduces arithmetic/lookup overhead without extra moving parts | Confirmed |
| The hot chunk path is losing more time to per-column object churn than to one more round of speculative noise caching | Replace the `ColumnContext[]` and nested column objects with a bounded scratch struct-of-arrays, then benchmark against the previous commit in a detached worktree | Confirmed. Direct A/B against `36ba50a` improved chunk generation from `317.4 ms -> 177.9 ms` on the sampled workload while preserving checksums | Confirmed |
| Hoisting per-y invariants like `floor(worldY / 3)` and the strata band base will matter inside the voxel loop | Fold those values out of the inner `x` loop and keep the same chunk output checksums | Confirmed. The flattened chunk path kept output identical and contributed to the same measured A/B win | Confirmed |
| `sampleColumn()` needs the same structural cleanup as chunk generation because far-field and spawn logic call it heavily | Benchmark `sampleColumn()` across a large fixed grid against the previous commit in the detached worktree | Confirmed. The sampled column benchmark improved from `340.4 ms -> 246.4 ms` with identical checksums | Confirmed |
| The local warmed stream profiler is enough to reject the generator rewrite if it destabilizes the broader runtime, even if Chrome tracing is temporarily unavailable | Re-run `profile-game-stream` after the rewrite and compare whether totals stay in the same broad range while correctness tests remain green | Confirmed. The warmed streaming profile remained stable enough to keep the rewrite while the direct worktree A/B showed clear generator-path wins | Confirmed |

## 2026-03-11 mesher neighbor lookup search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The next low-risk mesher win is repeated neighbor/world lookup overhead rather than another full meshing algorithm rewrite | Inspect `buildChunkMesh()` and compare the hot loop against the earlier trace buckets plus explorer guidance | Confirmed. The mask fill and occlusion paths were still paying repeated neighbor lookups and bounds fetches | Confirmed |
| Resolving the current chunk’s six neighbors once per mesh build will materially reduce raw meshing time | Replace the boundary `world.getVoxel(...)` fallbacks with pre-resolved neighbor chunk data, then A/B the same synthetic mesh workload against the previous commit | Confirmed. The fixed workload improved from `152.5 ms -> 111.9 ms` with identical checksum | Confirmed |
| Reusing those same resolved neighbors in the full-occlusion test is worthwhile instead of doing another pass of world lookups | Fold the occlusion test onto the same neighbor resolution and rerun mesher tests plus the A/B benchmark | Confirmed. Correctness stayed green and the same mesher A/B win was large enough to keep the shared neighbor path | Confirmed |
| Avoiding cloned `solidBounds` on the clean fast path is too small to matter | Let meshing use in-chunk clean bounds directly and compare the same benchmark/profile results instead of dismissing it | Rejected as “too small to care about”. It is cheap to keep, removes allocation churn, and fits naturally into the same mesher cleanup slice | Rejected |
| The warmed local game-stream profiler will show whether this mesher cleanup matters beyond a synthetic microbenchmark | Re-run `profile-game-stream` on the same `crossing-d2` configuration after the mesher rewrite | Confirmed. `totalMeshMs` improved from about `257.3 -> 179.2` while correctness tests remained green | Confirmed |

## 2026-03-11 far-field exclusion fast-path search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The far-field exclusion path is still doing unnecessary work for the common single-column case | Re-read `intersectsResidentColumns()` and `intersectsColumnKeySet()` and compare that logic to how coarse cells are sampled | Confirmed. The hot path was still taking the generic min/max nested-loop route even when one column key was enough | Confirmed |
| A single-column fast path is too small to matter and should be skipped in favor of a bigger representation rewrite | Add the fast path and rerun the same warmed `crossing-d2` profile before deciding | Rejected. The change is tiny and still improved `totalFarFieldMs` from about `82.0 -> 77.2` and `maxFrameWorkMs` from about `38.4 -> 37.0` | Rejected |
| The resident-column and render-ready-column paths both need the same fast-path treatment to avoid asymmetry bugs | Apply the fast path to both functions and rerun focused LOD/render-ready tests | Confirmed. Focused masking and LOD coverage tests stayed green after updating both paths together | Confirmed |

## 2026-03-11 biome rehaul search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| A richer biome system can be added without returning to arbitrary selector boundaries if terrain parameters are blended from the strongest base-biome influences | Replace the old selector-driven biome choice with field-scored base biomes plus soft transition materials, then run boundary-jump and forbidden-adjacency probes | Confirmed. Soft-edge and adjacency probes now pass with the richer biome roster | Confirmed |
| Nested/special biomes should be driven by host-biome + field rules rather than by standalone selector noise | Add `marsh`, `ember`, and `bloom` through host-rule thresholds and verify them with deterministic biome probes over a large grid | Confirmed. Host-rule tests now pass and all three special biomes appear for the fixed seed | Confirmed |
| Landmark/object generation can be introduced without breaking the current resident-world contract as long as the column API grows a cheap vertical envelope instead of changing the meaning of `surfaceY` | Add landmark/object silhouettes plus a `topY` field, then rerun resident-world and far-field tests | Confirmed. The repo stayed green after switching vertical residency to respect `topY` | Confirmed |
| Broad biome richness would silently regress the live world if spawn and vertical residency keep assuming the old biome names and flat surface-only columns | Re-read the live callers, then update spawn preferences and Y-range calculation in the same slice | Confirmed. Spawn preferences were updated to the new biome roster and residency now accounts for landmark height | Confirmed |

## 2026-03-11 global terrain envelope follow-up

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| A "global heightmap" will still create clean biome borders if it contains strong fast mountain signal instead of only slow continental structure | Re-run the fixed-seed boundary probe after the first rewrite and inspect the worst pair jumps plus the sampled field values on those edges | Confirmed. The first pass still produced very large jumps because `globalHeight` was reacting too strongly to fast mountain signal | Confirmed |
| Tightening the slow global envelope and pushing mountain/mesa intensity back into local relief will reduce border cliffs without flattening the whole world | Remove strong mountain contribution from `globalHeight`, reduce global height amplitude, and rerun the terrain-envelope and boundary-jump probes | Confirmed. The retuned generator now lands at `1372..1635` surface height with `44` max sampled biome-boundary jump | Confirmed |
| Height suitability needs to matter more in biome scoring or lowland biomes will still win on obviously high terrain | Narrow base-biome preferred height ranges, increase `globalHeight` score weight, and rerun the probe that previously found `steppe` on mountain-scale terrain | Confirmed. The bad lowland-on-high-terrain cases dropped out once height suitability was strengthened | Confirmed |
| Mesa/badlands/highland identity can stay strong without causing hard biome-edge cliffs if biome-local shaping is suppressed near borders | Key local relief and terrace strength to a stricter `biomeCore` instead of applying full biome shaping everywhere | Confirmed. Soft-boundary jumps fell back into the low-40s while interior biome identity stayed intact | Confirmed |
| Ground detail still will not feel like a 10 cm world unless surface material variation is explicitly tested | Add a material-diversity regression over a broad fixed grid instead of trusting visual intuition | Confirmed. The new test proves multiple surface variants exist within major biomes | Confirmed |
| Richer landmark variety requires a new object system before it is worth doing | Replace the single hardcoded landmark family with deterministic per-biome rosters and extend the existing parametric feature shapes | Rejected. The current one-feature-slot system was enough to add real variety cheaply | Rejected |
| The jump-height request is too subjective to test cleanly | Add a simple takeoff-to-peak regression in world units and verify the requested increase numerically | Rejected. A direct peak-height test is simple and now guards the new `4.2 m/s` jump | Rejected |

## 2026-03-11 landmark scale and terrain-detail follow-up

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The remaining object-scale complaint is mostly a profile-data problem, not a missing-geometry-system problem | Expand landmark selection from ids to placement profiles and resample tall landmark envelopes over a broad fixed-seed grid | Confirmed. The broad scan now finds a tallest sampled landmark height of `97` voxels (`9.7 m`) without adding a second object pipeline | Confirmed |
| More landmark ids alone will not produce the biome variety the user asked for if density and scale stay miniature | Add biome-specific `cellSize/chance/scale/variant` overrides and aggregate landmark counts by biome/id | Confirmed. The first roster rewrite still overproduced small flowers/shrubs; the kept version only became acceptable after density retuning toward larger landmark families | Confirmed |
| Terrain still will not read as a `10 cm` world if the generator only changes surface materials and keeps geometry too smooth | Raise biome micro-relief and add a slow shared `massifRelief` term, then rerun the fixed-seed terrain envelope probe | Confirmed. The probe kept soft-border jumps bounded while lifting peak sampled terrain to `1652` and adding more geometric relief | Confirmed |
| A second jump-height increase can be verified objectively instead of argued from feel | Raise jump velocity and rerun the peak-height unit test | Confirmed. The new `4.7 m/s` jump clears the updated `1.0 m` regression | Confirmed |

## 2026-03-11 transparent water and submersion seam

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| Water should stop being treated like generic solid terrain before either collision or rendering will feel correct | Add explicit `isCollisionMaterial(...)` / `isWaterMaterial(...)` seams and rerun focused physics + mesher tests | Confirmed. Once water was classified explicitly, physics and meshing became straightforward and the focused suite stayed green | Confirmed |
| A first usable water pass does not need full generic transparency or full voxel-volume water meshing | Split water into a separate top-surface mesh and render it in a blended pass, then verify terrain remains visible below water in the unit seams | Confirmed. The engine now emits water into a separate mesh and keeps terrain in the opaque pass without needing a generic OIT system | Confirmed |
| Global-water submersion can be verified without opening a heavy browser session | Add a player-physics regression for entering water and checking `bodyInWater` / `eyeInWater` | Confirmed. The new regression catches submersion numerically without relying on a human visual check | Confirmed |
| Keeping water palette alpha implicit in the renderer is too fragile for future work | Encode translucency directly in the procedural palette for water-class materials and verify it with a direct palette test | Confirmed. The procedural-water palette test now guards the alpha seam directly | Confirmed |

## 2026-03-11 landmark shape and underwater placement cleanup

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The grassy-underwater complaint is a real generator bug rather than a rendering artifact | Run a broad fixed-seed scan of underwater columns and count how many still expose grass-like top materials | Confirmed. Before the fix there were tens of thousands of underwater grass columns; after the change the same scan reports `underwaterGrass = 0` | Confirmed |
| Trees looking like poles is mostly a placement-footprint bug | Inspect landmark placement bounds and compare them to the actual configured silhouette radii before changing anything else | Rejected. The current complaint was mostly shape math, not placement clipping, so the speculative footprint-widening change was removed | Rejected |
| The real tree issue is fixed-width trunks plus crowns that start too early or stay too small for tall silhouettes | Update trunk-width and crown-profile rules for representative tree families, then measure fixed-seed crown breadth numerically | Confirmed. Representative `oak`, `fir`, `tall_fir`, and `palm` samples now show broad crowns instead of near-column silhouettes | Confirmed |
| The boulder complaint is a true profile bug in the cap/body relationship | Probe a representative boulder and compare the cap width against the body below it | Confirmed. The old cap rule could flare wider than the body; the kept rounded-body profile removes that failure mode and now has a direct regression | Confirmed |
| "No trees in water" can be validated numerically without opening a browser | Scan underwater columns for vegetation landmarks and check whether any trunk/root voxels occupy the first few layers above the submerged surface | Confirmed. The fixed-seed scan now reports `rootedInWater = 0` | Confirmed |

## 2026-03-11 grove/orchard landmark variety expansion

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| More variety should come first from patch-scale placement regions, not from adding many more top-level biome ids | Add slow `grove` / `orchard` / `desolation` fields and let them swap landmark rosters inside the existing biome ids | Confirmed. The kept slice creates dense groves, orchard/blossom pockets, and desolate dead-tree regions without expanding the exposed biome enum | Confirmed |
| A few new high-signal landmark families will matter more than a large pile of tiny decorative variants | Add `redwood`, `blossom_tree`, `fruit_tree`, `dead_tree`, and `berry_bush`, then run a broad fixed-seed landmark scan | Confirmed. All five appear in the broad scan and add clearly different silhouettes/patterns | Confirmed |
| The first grove/orchard tuning might still be too weak to matter while walking | Measure best fixed-seed tall-forest and orchard patch coverage ratios before locking the pass | Confirmed. The first tuning only reached about `18.3%` forest coverage and `14.8%` orchard coverage, so I did not keep it as-is | Confirmed |
| Stronger rosters plus slightly more permissive patch thresholds will create the dense-forest effect without needing a second object system yet | Retune the rosters, rerun the same fixed-seed patch probes, and require explicit density regressions in tests | Confirmed. The kept version reaches about `38.5%` dense-forest coverage and `17.8%` orchard coverage | Confirmed |
| Redwoods need an explicit scale regression or they will drift back toward "just another tall tree" | Run a broad fixed-seed scan and assert the tallest sampled redwood exceeds a true far-visible height threshold | Confirmed. The kept slice reaches `204` voxels (`20.4 m`) | Confirmed |

## 2026-03-11 water absorption and underwater render environment

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The "transparent deep water" complaint is mostly because water uses one fixed alpha regardless of depth | Add a depth-tint helper, drive it from near and far water surfaces, and require a test where deeper water emits a larger vertex alpha than shallow water | Confirmed. The focused integration test now sees different alpha values, and the numeric probe shows `176 -> 184 -> 208` alpha across shallow/medium/deep samples | Confirmed |
| The underwater view problem is a missing render-environment seam, not a reason to rewrite the whole transparency path | Add an explicit underwater render environment with tinted clear/fog color and shorter fog distance, then verify it numerically in a small helper test | Confirmed. Underwater rendering now has its own fog/clear setup and the helper test guards it directly | Confirmed |
| The first water-depth quantization is probably "good enough" and not worth revisiting | Run the mesher integration test with shallow vs deep water surfaces and see whether they actually produce distinct alphas | Rejected. The first quantization was too coarse, so I tightened it rather than weakening the test | Rejected |

## 2026-03-11 rare peak terrain follow-up

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| A new rare-peak field is automatically worthwhile if it sounds aligned with the roadmap | Add the field, rerun the broad fixed-seed terrain scan, and require a new high-peak regression | Rejected. The first mask was too narrow and left the broad scan unchanged at about `1652` max surface height | Rejected |
| Stronger spike-style peak math is the fastest way to get tall mountains without real downside | Retune the mask aggressively and rerun the terrain-envelope plus soft-boundary probes | Rejected. The overshoot pass produced `2000+` terrain and huge boundary jumps, so it was discarded | Rejected |
| The right mountain extension is a broad province lift plus a smaller crown term driven by slow shared fields | Replace the spike mask with a slower `peakness` province, rerun the broad fixed-seed terrain scan, and keep it only if rarity and soft-edge budgets stay healthy | Confirmed. The kept version reaches `1762` max sampled terrain, keeps `1700+` terrain extremely rare, and preserves the existing soft-edge budgets | Confirmed |

## 2026-03-11 old-growth and landmark-variety follow-up

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| More variety now mainly requires more top-level biome ids | Add one more slow regional placement field plus several themed landmark rosters inside existing biomes, then rerun the broad landmark scan | Rejected. The existing biome ids were enough; regional landmark overlays gave the extra variety more cleanly | Rejected |
| New landmark ids are enough on their own, even if they reuse old feature shapes without silhouette-specific tuning | Add `willow`, `giant_flower`, and `thorn_tree`, then check representative shapes and broad landmark presence | Rejected. The new families only became worth keeping after the reused feature paths got willow- and flower-specific silhouette tweaks | Rejected |
| Dense old-growth forests are better expressed as region overlays than as globally denser default biomes | Introduce an `oldGrowth` field, swap to denser rosters only inside those regions, and verify forest patch ratios stay high without flattening the whole biome roster | Confirmed. The kept version reaches about `42%` dense-forest coverage in the best patch while preserving broader biome contrast | Confirmed |
| Bloom flower-grove regions are already strong enough to justify a "dense glade" claim | Add a glade-pocket probe and compare it against an explicit threshold instead of trusting the new ids | Rejected. The glade pockets are real, but the measured best pocket is only about `14.8%`, so the stronger claim was dropped | Rejected |

## 2026-03-11 rare regional-extremes search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| More extreme biome moments now require more top-level biome ids | Add rare region overlays inside the current biome families instead of adding more public biome ids, then verify that each overlay appears in a broad deterministic scan | Confirmed. The overlay approach produced nine rare regional extremes while preserving the current smooth host-biome transition model | Confirmed |
| A highly multiplicative selector is the safest way to keep regional extremes rare | Probe broad fixed-seed maxima/counts for the first selector pass before committing to it | Rejected. The multiplicative pass made most variants effectively unreachable, so rarity became dead code rather than useful scarcity | Rejected |
| Averaged world-rule signals with per-host thresholds will keep regional extremes both real and rare | Switch selectors to averaged signals, retune thresholds from measured maxima, and rerun the broad rarity scan | Confirmed. All expected variants now appear while each stays far below the `2%` regression cap | Confirmed |
| Extreme-biome variety should affect more than landmark choice or it will read as a cosmetic footnote | Let the regional overlay also influence local relief, palette/material overrides, and special water behavior, then keep it only if the result still fits the existing generator architecture cleanly | Confirmed. The kept pass adds localized terrain/material identity without requiring a new structure system | Confirmed |
| A special highland red-leaf region can piggyback entirely on existing trees without a dedicated landmark family | Add the region with only palette-swapped existing families and see whether landmark coverage still reflects the intent cleanly | Rejected. The pass became clearer after adding `redleaf_tree` explicitly instead of pretending the region had unique canopy identity already | Rejected |

## 2026-03-12 larger biome-family expansion

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The next big variety win requires more actual biome families, not only more rare variants inside the existing roster | Add new base/special biome families from the biome doc and verify they all appear in a broad fixed-seed scan | Confirmed. The kept pass now exposes `16` top-level biomes including `savanna`, `moor`, `firefly`, `saltflat`, `fern`, `fungal`, and `shardlands` | Confirmed |
| New biomes can still fit the current architecture if they are implemented as host-rule transforms over the existing world fields | Drive the new specials from host-set strength plus field conditions and keep them only if soft-edge and forbidden-adjacency tests stay green | Confirmed. The generator gained many more biomes without reopening the old boundary-cliff problem | Confirmed |
| A few new landmark ids are enough to make the new biomes read distinctly if the rosters are right | Add `giant_fern`, `lantern_tree`, and `salt_spire`, then verify biome-specific landmark identities in broad scans | Confirmed. The new biome families now have measurable landmark identities instead of just material swaps | Confirmed |
| The first new-special selectors are probably safe if they look principled on paper | Probe the fixed-seed counts after the first implementation before keeping it | Rejected. The first pass dead-coded `firefly`, `fern`, and `fungal` almost completely because the selectors were too multiplicative again | Rejected |
| Averaged-signal selectors are the right fix for those dead new biomes too | Replace the multiplicative selectors for the new specials with averaged signals and rerun the broad scan | Confirmed. `firefly`, `fern`, and `fungal` became real biomes once the selectors were rewritten that way | Confirmed |
| The first wet-biome water tuning is probably good enough once the biome ids appear | Inspect deterministic biome/landmark identity scans for `fern` and `firefly` after the first implementation | Rejected. The first version submerged those biomes so heavily that they lost their intended vegetation and mostly read as reeds/boulders | Rejected |
| The right wet-biome fix is less automatic flooding plus stricter pool/cenote triggers | Reduce the default depression/water policy for `firefly` and `fern`, then rerun the landmark-identity scan | Confirmed. `fern` now actually yields `giant_fern`, and `firefly` now yields `lantern_tree` islands instead of collapsing into water-only clutter | Confirmed |
| Host-biome labels alone are the best regression for every new special biome | Keep the new test suite purely in terms of `hostBiomeId` membership | Rejected. For `shardlands`, the more stable regression is the actual dry/high-energy field conditions, not a brittle single host label | Rejected |

## 2026-03-12 recursive loop bootstrap

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| A single standard multi-front acceptance command will catch repo drift earlier than ad hoc per-slice commands | Add `mise run cycle-bench`, run it on the current tree, and keep it only if it surfaces issues or gives useful comparable metrics | Confirmed. The first run immediately exposed a dead generator helper plus two stale far-field tests, and the kept version now writes comparable timestamped reports | Confirmed |
| The settled-anchor LOD coverage test failure was a real engine regression | Re-run the focused file and inspect the failure mode before changing runtime code | Rejected. The failure was the default `5 s` suite timeout, not a new coverage bug, so I fixed the test contract instead of touching runtime code | Rejected |
| The far-field water preservation regression should still inspect the opaque mesh only | Compare the failing expectation to the current far-field mesh format after the separate water-mesh split | Rejected. The test had become stale after water moved into its own blended mesh and depth tint path | Rejected |
| Updating the water-preservation regression to read the water mesh and tinted water color will better match the actual renderer contract | Adjust the test to inspect `waterVertexData` and compare against `applyWaterDepthTint(...)`, then rerun the focused far-field suite | Confirmed. The focused suite now passes and the regression once again tests the current behavior rather than an obsolete path | Confirmed |

## 2026-03-12 exploration journal feature slice

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| A first exploration mechanic can be useful without inventing inventory, quests, or rewards yet | Add a pure exploration journal with deterministic bookkeeping and wire it into the game HUD/debug surface | Confirmed. The journal adds measurable progression immediately and stays small enough to test directly | Confirmed |
| The bookkeeping should live directly inside `GameController` because the runtime is the only caller today | Try the pure-state implementation first and keep it only if the journal remains easy to test and easy to integrate | Rejected. The pure helper is cleaner and gave immediate unit-test coverage without browser coupling | Rejected |
| Sampling only the exact player column is enough to discover landmark families in a satisfying way | Inspect generator semantics while wiring the journal and compare them to landmark placement patterns before freezing the implementation | Rejected. Landmark ids often live on nearby feature roots, so a tiny local sampling pattern is a better first seam than feet-only detection | Rejected |
| Throttled local discovery sampling will show up as a meaningful command-line regression | Run `mise run cycle-bench` after the runtime integration and compare it to the loop-bootstrap baseline | Rejected. The cycle battery stayed effectively flat, so the journal sampling is worth keeping | Rejected |

## 2026-03-12 browser route trace harness

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The repo can drive a fresh Chrome/WebGPU route benchmark and capture a real trace from one command instead of relying on manual DevTools sessions | Build a Bun script that launches a fresh server and Chrome, runs `benchmarkRouteExperience(...)` through CDP, captures a trace, analyzes it, and saves a report | Confirmed. `mise run trace-route -- --label=smoke` completed successfully and produced a combined report plus trace artifact | Confirmed |
| Headless latest Chrome will not run this repo's WebGPU route benchmark path reliably enough to be useful | Try the new harness directly against a fresh production server and fail the slice if WebGPU or the route benchmark does not come up | Rejected. Headless Chrome 146 worked for the smoke runs on this machine | Rejected |
| The first version can stay hard-coded because route tracing is rare | Add one smoke run, then try a second run with different duration/sample-rate options before keeping the command | Rejected. Parameterization mattered immediately, so the kept command now exposes duration, settle time, sample rate, speed, and capture/reference knobs | Rejected |
| A real browser trace will mostly repeat what the command-line profilers already say and add little value | Compare the route summary and trace hot spots to the local profile outputs | Rejected. The trace made the inclusive runtime stack much clearer and confirmed that browser-route work is dominated by residency + mesh flushing in exactly the path the player experiences | Rejected |
| Production-route traces will automatically have sufficiently readable symbols for deep hotspot work | Inspect the first smoke trace before locking the harness | Rejected. Enough names survived to make the harness useful, but several hottest frames are still minified, so a trace-friendly build/report mode is the next harness improvement | Rejected |

## 2026-03-12 denser landmark stands and underground surface signatures

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The next world-identity win needs more top-level biome ids again | Re-read the current biome notes and try a narrower implementation first: denser stand-scale landmark rosters plus underground-to-surface landmark leakage | Rejected. The kept improvement came from stronger placement regimes and cross-layer signals, not another expansion of the biome enum | Rejected |
| Underground biome families are currently too hidden to matter for exploration | Thread `undergroundBiomeId` into landmark roster selection and keep it only if fixed-seed scans show distinct landmark signatures by underground family | Confirmed. `rooted`, `granitic`, `saline`, `mycelial`, `crystalline`, and `basaltic` now all expose stable surface signatures | Confirmed |
| A couple of new landmark silhouettes are enough to make those underground surface cues legible | Add `root_stump` and `stone_tor`, then verify they actually appear across the broad scan | Confirmed. Both now appear in the fixed-seed world and participate in the intended rosters | Confirmed |
| The previous forest/orchard/glade pocket test is already a sufficient proxy for stronger biome clustering | Extend the deterministic probe to look for fern-jungle density too before deciding the current tests are enough | Rejected. The earlier test missed an important clustering regime; the kept suite now measures a dense fern/canopy pocket ratio as well | Rejected |
| Underground-surface leakage should override every biome once available | Implement the leak only in selected biome paths first and keep it if the signatures are present without flattening biome identity | Confirmed. Selective use was enough to make the signals real while preserving biome-specific landmark rosters | Confirmed |

## 2026-03-12 off-main-thread chunk generation

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The highest-signal next performance seam is moving chunk generation off the main thread while leaving meshing where it is | Add an optional browser worker queue for `generateChunk(...)` and keep it only if route traces improve without hole regressions | Confirmed. The kept worker path materially improved route-frame cost while staying hole-free | Confirmed |
| Bun's current full-stack/browser bundling path will automatically materialize a browser worker from `new Worker(new URL(\"./worker.ts\", import.meta.url), ...)` | Try the worker path directly and inspect the route trace if performance looks suspiciously good | Rejected. The first attempt produced a ghost win: pending chunks exploded, no detailed meshes were built, and hole signals reappeared because the worker asset was never really there | Rejected |
| A same-parameter browser A/B against a previous commit is unnecessary because the route trace itself is stable enough to eyeball | Run the same `trace-route` parameters from a temporary worktree at `c7c51cd` and compare reports directly | Rejected. The worktree baseline made the win much easier to defend and also prevented me from keeping the broken first worker attempt | Rejected |
| Once chunk generation is async, the current route benchmark will still meaningfully catch visual regressions | Compare the first worker attempt against the baseline through the route benchmark's hole counters | Confirmed. The benchmark immediately caught the broken worker asset path because hole signals rose to `35` | Confirmed |

## 2026-03-12 gather/build loop and persistence-ready edit overlays

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The first interaction slice should mutate procedural generation directly because persistence is still future work | Add an edit-overlay layer first and verify edits survive chunk eviction and regeneration without changing generator contracts | Rejected. The overlay model already solves the persistence seam cleanly and keeps worldgen deterministic | Rejected |
| Inventory pickup capacity should be modeled as only “room in the selected stack” because placement uses a single selected slot | Drive pickup checks through total per-material inventory capacity and verify merge/open-stack/exhaustion behavior in focused tests | Rejected. Total remaining material capacity across the whole inventory is the correct pickup primitive; only the first test expectation was wrong | Rejected |
| Break/place needs browser-driven acceptance before it is worth landing at all | Add deterministic unit coverage for inventory, raycast interaction, and overlay persistence first; keep the slice only if the full repo suite stays green | Confirmed. Stable deterministic coverage was enough to land the core seam cleanly, with a browser smoke queued as later harness work | Confirmed |

## 2026-03-12 async detailed chunk meshing with near-field continuity

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The highest-signal next throughput win after async chunk generation is to move detailed chunk meshing off the main thread too | Add a browser worker queue for detailed opaque chunk meshing and keep it only if route traces improve without hole regressions | Confirmed, but only after continuity fixes. The kept version materially reduced avg/p95 route frame cost while staying hole-free | Confirmed |
| Pure async meshing with the existing near-field/far-field policy will stay visually safe because render-ready masking already protects the seam | Run the first async-mesh route trace before adding any safety fallback | Rejected. The first pass was another ghost win: frame time improved, but hole signals rose to `16` because meshless near chunks could still outrun the detailed bubble | Rejected |
| Expanding the synchronous near-mesh safety budget alone is the right fix for those async-mesh holes | Increase the sync near-chunk bubble and rerun the same route trace until holes disappear or the main-thread cost erases the async win | Rejected. A larger sync bubble reduced holes but did not remove them on its own | Rejected |
| Reopening `near-transition` fallback coverage down to the player for unmasked columns will restore continuity without sacrificing most of the async-mesh win | Set `near-transition.innerRadius = 0`, keep render-ready masking, and rerun the route trace after the hybrid async/sync meshing policy | Confirmed. The kept route trace returned to `0` hole signals while preserving a large avg/p95 frame improvement | Confirmed |
| Route-trace reports are informative enough if they keep only the summary | Add persisted per-frame benchmark samples to the trace artifact and verify they are written during a short smoke run | Rejected. The summary alone was too lossy for future spike work, so the harness now also writes `benchmark-samples.json` | Rejected |

## 2026-03-12 persistence-oriented generated chunk storage

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| Generated chunks should stop crossing the worker boundary as raw dense arrays if we want persistence and later server streaming to stay sane | Add a compact generated-chunk codec and keep it only if it round-trips cleanly and materially compresses trivial chunks | Confirmed. Empty and uniform chunks now encode dramatically smaller while round-tripping full generated chunk data | Confirmed |
| A sparse outer format should start at the subchunk level instead of inventing a fully new resident-memory structure immediately | Encode chunks as `8^3` subchunks with `empty/uniform/palette/dense` modes and keep it only if it stays simple enough to decode into the current resident world | Confirmed. The codec is small enough to keep and does not force a resident-world rewrite yet | Confirmed |
| Far-field should move to a lighter query mode of the same generator instead of drifting into a custom far generator | Add `sampleSurfaceColumn(...)`, switch far-field to it, and keep it only if the relevant surface fields match `sampleColumn(...)` in tests | Confirmed. The lighter path is now the far-field source and the surface-field equivalence test stays green | Confirmed |
| Silent cache-failure catches are acceptable because the persistent cache is just an optimization | Inspect the worker cache boundary after the first implementation and keep the catches only if they are at least externally visible | Rejected. The silent catches were removed; cache open/read/write failures now log once and disable the cache explicitly | Rejected |
| A cache-reuse browser benchmark is only worth keeping if it works on the first try in headless mode | Add the benchmark seam and try a first headless proof immediately | Rejected. The first proof attempt exposed a readiness/harness issue rather than providing trustworthy evidence, so the seam stays but the proof is not counted as acceptance yet | Rejected |

## 2026-03-12 shared surface summary cleanup and spawn support fix

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The duplicated surface pipeline in `sampleSurfaceColumn(...)` and `fillColumnState(...)` can be collapsed without changing behavior | Refactor both onto one shared surface-summary path, then rerun the generator/resident/far-field test suites | Confirmed. The duplicate logic is gone and the focused suites stay green | Confirmed |
| Once the logic is shared, `sampleSurfaceColumn(...)` is still the right generic cheap probe for resident-world hot paths | Microbench `sampleSurfaceColumn(...)` vs `sampleColumn(...)` over a deterministic grid before keeping the caller changes | Rejected. After the cleanup, `sampleSurfaceColumn(...)` was slower, so I moved resident-world spawn/y-range callers back to `sampleColumn(...)` | Rejected |
| The resident-world test failures after the refactor were just stale assumptions about the new sampling API | Inspect the first failed spawn-support load directly by probing the generated chunk and sampled materials | Rejected. The failure exposed a real bug: spawn selection was willing to choose cave-breached “surface” columns with no true support | Rejected |
| The right spawn fix is to keep trusting the procedural surface sample but widen the residency/support heuristics | Probe the chosen spawn column and compare it to actual sampled solids/headroom before changing runtime policy | Rejected. The coherent fix was to make spawn search require a real standable floor with headroom inside the footprint | Rejected |
| Spawn support should now be guarded by a direct regression instead of indirectly through residency expectations | Add a focused spawn-footprint support test after the runtime change | Confirmed. The new regression now catches unsupported cave-mouth spawn candidates directly | Confirmed |

## 2026-03-12 generated-chunk-only far-field rendering

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The far-field can be switched off generator sampling cleanly if generated chunks carry enough derived summary data | Add chunk-carried surface summaries, make `ProceduralFarField` depend on a narrow source interface, and rerun far-field + browser smoke coverage | Confirmed. The far renderer no longer imports the generator and the browser smoke stayed hole-free | Confirmed |
| It is acceptable to keep deriving far-field summaries from procedural callbacks as long as render-time calls disappear | Compare a generator-derived summary seam to a chunk-byte-derived summary seam before keeping one | Rejected. The kept path derives summaries from actual generated chunk bytes so resident edits can flow into far-field data too | Rejected |
| Once far-field reads generated summaries, the world does not need to keep any post-eviction summary archive | Switch the far-field source to the world without persisting generated summaries beyond residency and look for missing coverage | Rejected. The world must retain chunk summaries after eviction or the far field immediately loses already-generated terrain | Rejected |
| A summary-backed far-field path can rely on existing resident generation alone and does not need a separate pre-generation bridge | Wire the world source directly and inspect coverage before adding any prefetch behavior | Rejected. The far field needs conservative non-resident surface prefetch or the horizon collapses to resident-only data | Rejected |
| The old broad LOD continuity integration test is still the right oracle after the source architecture changes | Re-run it unchanged first | Rejected. The kept oracle is now a bounded summary-backed continuity test plus direct world-summary tests, which match the new contract much better | Rejected |

## 2026-03-12 chunk-derived render summary architecture

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| A top-surface summary is sufficient as the permanent far-field seam once rendering no longer samples the generator | Re-evaluate the repo architecture against the research docs and the current chunk/persistence goals, especially huge edited caverns | Rejected. Surface-only summaries are a tactical seam, not the end state | Rejected |
| The right long-term derived chunk metadata should include volumetric occupancy information even before a volumetric far renderer exists | Extend the chunk summary to carry coarse occupancy states and boundary air openings, then keep it only if codec/tests still stay lean | Confirmed. The new `GeneratedChunkRenderSummary` keeps the current surface path working while adding the missing volumetric seam | Confirmed |
| Empty chunks can continue to be modeled as “no summary” because they do not render today | Add focused tests for empty-chunk render summaries and evaluate the underground/cavern implications before freezing the contract | Rejected. Empty chunks are part of the authoritative visibility state and now keep explicit empty summaries | Rejected |

## 2026-03-12 summary-only far prefetch

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| Far prefetch should keep requesting full generated chunks because the worker/cache path already exists and the extra summary mode is needless complexity | Add a summary-only worker/request path and keep it only if the build, route smoke, and focused chunk/LOD suites stay clean | Rejected. The summary-only path is the better seam because far work no longer needs to decode/adopt full chunks just to archive render metadata | Rejected |
| “For caves” is an acceptable framing for the upcoming volumetric far renderer | Re-read the architecture docs against the edited-world requirement before keeping that language | Rejected. The kept wording is now arbitrary large interior/void spaces in an edited world, not generator-known caves | Rejected |

## 2026-03-12 persisted summary records

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| A separate persisted summary store is premature because summary requests can always decode from the cached chunk payload | Add a dedicated summary store and keep it only if the build and browser route smoke stay clean | Rejected. Persisted summaries are the better long-term seam because summary-only requests can now stay summary-only all the way to storage | Rejected |
| Splitting chunk payloads and summary records in IndexedDB will complicate the worker too much for the value | Keep the chunk store as fallback, backfill missing summaries on demand, and validate the route smoke before deciding | Rejected. The fallback/backfill path stayed simple enough and the smoke stayed healthy | Rejected |

## 2026-03-12 off-main-thread generated chunk adoption

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The highest-signal gameplay hitch seam in procedural generation is the main-thread decode that happens when generated chunks drain into residency | Replace encoded chunk completions with worker-decoded typed-array completions and keep the change only if browser streaming traces improve without hole regressions | Confirmed, but only as a gameplay-path win. The kept route trace improved while staying hole-free | Confirmed |
| A more aggressive default generation worker pool should be a free win once chunk decode leaves the main thread | Raise the default pool to `4` workers and rerun the startup + short walk browser benchmark | Rejected. Cold start regressed hard because the worker pool saturated the machine | Rejected |
| The cold-start regression after raw chunk transfer is mostly because empty chunks are now crossing the worker boundary as dense zero-filled buffers | Omit dense voxel payloads for empty chunks, rerun the same startup + walk benchmark, and keep the change only if the regression shrinks without hurting route traces | Confirmed. Empty-chunk elision materially reduced the regression and is worth keeping even though startup is still slower than the old encoded baseline | Confirmed |
| If startup is still slower after the empty-chunk fix, then “more worker threads” is the wrong next move | Compare the current browser startup benchmark to the current browser route trace instead of averaging them together | Confirmed. Startup is still worse, but the route trace improved enough to justify the gameplay-focused slice and points clearly at cache persistence as the remaining coupling | Confirmed |

## 2026-03-12 deferred chunk-cache persistence

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| Inline cache encode/write on the generation workers is now the main remaining startup bottleneck after raw chunk handoff and empty-chunk elision | Move fresh chunk persistence behind chunk delivery with a small delayed worker-side queue, then rerun the same startup + walk browser benchmark | Confirmed. Startup visual-ready improved materially, so inline persistence really was part of the problem | Confirmed |
| A separate summary-backfill path is harmless even if it is not coordinated with deferred full chunk writes | Implement the first deferred writer with two partially overlapping timer paths and inspect the logic before benchmarking | Rejected. That version was brittle and could lose summary writes; the kept design uses one unified persistence queue | Rejected |
| The one-second forward-walk browser benchmark is sufficient to judge the deferred persistence slice by itself | Compare the new startup/walk microbench against the current route trace before deciding | Rejected. The walk microbench got noisier, while the route trace and startup benchmark both improved, so the microbench is not the primary oracle for this slice | Rejected |

## 2026-03-12 bounded async chunk adoption backlog

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The remaining “walk a few steps and freeze” regression is not generation itself but the main-thread promotion of already-completed worker chunks | Read the residency path directly, then add a focused test with a large completed-chunk backlog and keep the change only if adoption becomes budgeted across frames | Confirmed. Completed async chunks were bypassing `maxGenerateChunks`, and the kept ready-backlog path now enforces that budget | Confirmed |
| The async queue itself must be redesigned before the hitch can be fixed | Try the smaller resident-world-side backlog first and see if route traces recover without queue surgery | Rejected. The smaller fix was enough for this regression, so I did not add more queue complexity yet | Rejected |
| Keeping completed generated chunks around after the player moves away is harmless because they may be useful soon | Inspect the ready-backlog lifetime after the budget fix and keep it only if stale chunk payloads do not accumulate | Rejected. The kept design drops no-longer-needed ready chunk payloads while preserving their derived summaries | Rejected |

## 2026-03-12 budgeted bootstrap and playable-ready startup gate

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The giant startup time is mostly a real bootstrap cost in the first frame | Replace nothing yet, just change the startup benchmark to stop at a local playable bubble and compare the number | Rejected. The benchmark definition itself was badly overstating startup by waiting for the full residency/far bubble | Rejected |
| Even after fixing the benchmark definition, `loadBootstrapWorld()` is still doing too much synchronous work up front | Inspect the startup call path directly and compare `syncWorldAroundPlayer(true)` versus the budgeted path | Confirmed. Bootstrap was forcing an infinite settle pass on page load | Confirmed |
| The right startup model is a bounded local bubble plus progressive streaming, not an eager full settle | Switch bootstrap to the normal budgeted sync path and keep it only if startup entry time collapses without introducing hole regressions in route traces | Confirmed. Playable-ready dropped to about `1.43 s` and route traces stayed hole-free | Confirmed |
| A good “enter the world” gate should still wait for every pending chunk in the full residency radius | Re-evaluate readiness using a local support/column/urgent-mesh bubble around the player instead of global pending counts | Rejected. Global pending counts are the wrong readiness gate for gameplay entry | Rejected |

## 2026-03-12 cached column y-ranges for residency streaming

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| Repeated `computeChunkYRange()` calls are still wasting CPU on deterministic column work across frames | Add a focused resident-world regression that repeats the same anchor update and only keep the cache if `sampleColumn()` calls collapse on the second update | Confirmed. The second update now only pays the center surface probe | Confirmed |
| This cache is too speculative to matter after the playable-ready bootstrap change | Keep the runtime change only if a full 10-second route trace moves materially, not just a micro test | Rejected. The route trace moved hard enough to justify the cache | Rejected |
| A startup-only browser benchmark should be the acceptance oracle for this slice too | Run it once and keep it if it behaves | Rejected. That harness run got stuck during teardown, so I did not count it as evidence | Rejected |

## 2026-03-12 cached and simplified spawn search

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The next startup hotspot is the spawn search itself, not only the later bootstrap residency work | Inspect the trace hot path, then directly count `sampleColumn()` calls for one `getSpawnPosition()` on a deterministic seed | Confirmed. One spawn search was costing about `580` `sampleColumn()` calls before this slice | Confirmed |
| The right fix is to search fewer rings around the origin | Try the reduced-ring patch first and compare it to the broader spawn requirements before keeping it | Rejected. That narrows correctness margin for a small gain and does not address repeated lookups | Rejected |
| A safer win is to cache the resolved spawn and remove obvious duplicate spawn-search sampling | Cache `getSpawnPosition()`, dedupe `spawnSearchOffsets(...)`, skip re-sampling the footprint center, then rerun the startup benchmark | Confirmed. The first spawn search dropped to about `450` `sampleColumn()` calls, later spawn lookups became free, and startup playable-ready improved to about `947.6 ms` | Confirmed |

## 2026-03-12 defer far-summary prefetch until after world entry

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| Far-summary prefetch is still doing measurable CPU work during startup, but the benchmarks do not isolate it clearly enough yet | Add explicit far-summary prefetch timing/request counters to startup and route benchmark samples before changing policy | Confirmed. The new metrics are now part of the benchmark payloads and CSVs | Confirmed |
| It is worth doing far-summary prefetch before the world is even playable because the horizon needs to be ready immediately | Block prefetch until `bootstrapPlayableReady`, rerun startup and route smokes, and keep it only if entry time improves without reintroducing hole signals | Rejected. Prefetch before entry was wasting startup budget; deferring it cut playable-ready from about `947.6 ms` to about `273 ms` while route hole signals stayed at `0` | Rejected |
| Even after entry, far-summary prefetch should still compete with chunk adoption backlog | Gate prefetch off when the resident-world ready backlog is non-zero and observe the startup/route behavior | Confirmed. The kept policy avoids piling horizon work on top of already-finished nearby chunk adoption | Confirmed |

## 2026-03-12 tracked dirty-chunk sets for meshing

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The next meaningful main-thread meshing tax is not the mesh math itself but rediscovering dirty chunks every frame by scanning the full resident set | Re-read the trace, then replace dirty-chunk rescans with a tracked dirty set and rerun focused tests plus a route smoke | Confirmed. The route smoke improved and `flushMeshBuildBudget(...)` dropped out of the top trace frames | Confirmed |
| This bookkeeping refactor will probably hurt startup enough that it is not worth the steady-state win | Re-run the startup benchmark after the dirty-set change instead of assuming it is free | Rejected. Startup stayed roughly flat, so the bookkeeping seam is worth keeping | Rejected |
| Dirty-mesh summaries and urgent-meshless checks can keep rescanning all resident chunks because only the mesher itself is hot | Switch those helpers to the tracked dirty iterator too and keep it only if tests and route smoke stay clean | Confirmed. The helpers now follow the same tracked-dirty path and the runtime stayed healthy | Confirmed |

## 2026-03-12 live-walk benchmark and movement-phase far-prefetch

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The user-reported walking freeze is real, but the current synthetic walk benchmark is the wrong oracle because it teleports the player and bypasses the live RAF path | Add a live-walk benchmark that uses the actual interactive loop, then compare it to the existing deterministic walk benchmark | Confirmed. The live-walk benchmark immediately reproduced the freeze while the deterministic walk benchmark stayed deceptively fast | Confirmed |
| The stall is mostly near-field generation/adoption/meshing, not far-summary work | Inspect the first live-walk trace and benchmark samples before changing policy | Rejected. The smoking gun was move-phase far-summary prefetch: about `8285 ms` max and `25.35 ms` average during movement | Rejected |
| It is acceptable to keep doing far-summary prefetch during held movement because it is “just horizon work” | Block far-summary prefetch while movement intent is active, rerun the same live benchmark and trace, and keep it only if the catastrophic spike disappears without hole regressions | Rejected. Move-phase prefetch was the bug; removing it during movement cut move-phase max gameplay frame from about `8286.9 ms` to about `12.1 ms` with `0` hole-signal frames | Rejected |
| Once move-phase prefetch is gone, the walking path should be interactive again even if the far-summary algorithm itself remains expensive | Re-run the live-walk benchmark and inspect the move-phase samples specifically | Confirmed. The live-walk path is now in low single-digit milliseconds during movement | Confirmed |

## 2026-03-12 incremental far-summary frontier prefetch

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The remaining non-movement far-summary cost comes mostly from broad repeated scanning, not from the actual requested summary count | Replace the restart-from-center scan with a stateful cursor plus bounded per-call visits, then compare live-forward Chrome traces instead of just average frame summaries | Confirmed. The reported exclusive prefetch hotspot time dropped from about `4461.1 ms` to `0 ms` in the new live-forward trace | Confirmed |
| Newly completed region/chunk summaries should seed localized follow-up work directly instead of waiting for another broad scan to stumble across them | Add a candidate-column queue driven by persisted-region adoption and chunk-summary updates, then lock it down with a resident-world regression | Confirmed. The new regression proves persisted region summaries now seed incremental chunk-summary discovery without generator probing | Confirmed |
| Resetting the search cursor on anchor changes can safely discard queued frontier work because the next broad scan will rediscover it quickly enough | Keep the first cursor-reset implementation only if the new incremental regression passes | Rejected. The reset cleared useful queued work from completed persisted summaries; the kept version preserves queued candidates and only reseeds pending-range columns | Rejected |
| The more incremental prefetch path might improve traces but still secretly hurt real walking or hole correctness | Re-run the 10-second live forward-walk benchmark and keep the refactor only if hole signals stay at `0` and max frame time stays in the interactive range | Confirmed. The walk benchmark stayed hole-free with about `11.9 ms` max gameplay frame | Confirmed |

## 2026-03-12 summary-only deferred persistence for far-field requests

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| Summary-only far-field requests are still paying too much worker cost because they persist full chunks instead of only the summary data they actually need | Change summary misses to persist only `putChunkSummary(...)`, then compare the live walk benchmark and Chrome trace against the incremental-prefetch baseline | Confirmed. Walk max gameplay frame dropped from about `11.9 ms` to `9.5 ms`, and persistence hotspots dropped out of the reported top exclusive trace frames | Confirmed |
| The deferred persistence queue is doing redundant work because repeated summary jobs and later chunk jobs for the same coord accumulate independently | Replace the ad hoc array queue with a tested deduping queue where chunk jobs supersede summary jobs for the same coord | Confirmed. The pure queue tests passed and the runtime stayed healthy | Confirmed |
| Persisting full chunks for summary-only requests is necessary to preserve the current world model | Keep the summary-only version only if far-field correctness and resident-world tests stay green | Rejected. The far field only needs persisted summary truth for these requests, and all focused tests stayed green | Rejected |
| The observed trace improvement might only be noise from a single run and not reflect the persistence change at all | Compare the reported trace hotspot sets directly, not just average frame numbers | Confirmed. `transaction @ :` and `postMessage @ :` were present in the earlier top exclusive frames and absent from the new trace’s reported hotspot set | Confirmed |

## 2026-03-12 split surface and cave field sampling

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| Cave-mouth boundary softening is still doing redundant biome-selection work inside `configureCaveState(...)` | Replace the neighbor-biome reclassification probe with a suppression factor derived from `biomePrimaryWeight`, then rerun the cave-boundary generator tests | Confirmed. Cave-boundary and cave-mouth tests stayed green while the generator microbench moved sharply | Confirmed |
| Surface-only column queries are still paying cave-noise cost they never use | Split `sampleFields(...)` into `sampleSurfaceFields(...)` and `sampleCaveFields(...)`, then compare the fixed generator microbench checksum and timings | Confirmed. The checksum stayed stable and `sampleColumnGridMs` dropped from about `98.429 ms` baseline to about `21.827 ms` after the split | Confirmed |
| The duplicated cave-field zeroing in `fillSurfaceColumnState(...)` and `fillColumnState(...)` is harmless enough to ignore | Remove it as part of the field split and keep the change only if type/tests/browser acceptance stay green | Confirmed. `configureCaveState(...)` already writes the full cave output set, so the double reset was dead work | Confirmed |
| The generator microbench might improve while real walking gets worse, making the split a false win | Re-run the 10-second forward-walk benchmark and the live-forward trace, and reject the slice if holes or frame spikes regress | Rejected. Walk stayed hole-free and max gameplay frame improved from about `10.3 ms` to `9.6 ms`; the trace remained hole-free and max gameplay frame improved from about `12.5 ms` to `10.8 ms` | Rejected |
| The build-based browser benchmark and route trace harnesses are safe to run in parallel | Try it once and treat any failure as a harness bug, not as evidence for or against the optimization | Rejected. Parallel runs raced on `dist/` removal, so the accepted browser verification was rerun sequentially | Rejected |

## 2026-03-12 generator allocation cuts after rejecting the envelope path

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| A lightweight column-envelope sampler for residency Y-range work is the right next step after the surface/cave split | Add `sampleColumnEnvelope(...)`, wire it into residency Y-range and center-surface sampling, and keep it only if the 10-second live-walk benchmark improves | Rejected. Both envelope attempts looked fine in microbenches but regressed the real live-walk benchmark, so I removed the whole slice | Rejected |
| The accepted trace is now pointing at per-column allocation churn in surface-biome classification and material resolution | Remove the `specialCandidates` array/object allocation in `selectBiomeClassification(...)` and replace `resolveSurfaceMaterials(...)` allocation with a reusable scratch bundle | Confirmed. The generator checksum stayed stable while the microbench improved again and the live-forward trace average dropped from about `1.469 ms` to `1.374 ms` | Confirmed |
| These allocation cuts might only help microbenches and disappear in the real browser route | Re-run the 10-second walk benchmark and live-forward Chrome trace, and reject the slice if hole signals regress or the browser metrics move the wrong way | Confirmed. Walk stayed hole-free, `avg_deltaTaskDurationMs` improved from about `3422.525` to `3406.150`, and the trace stayed hole-free | Confirmed |
| `selectBiomeClassification(...)` and `resolveSurfaceMaterials(...)` are still worth special-casing even if they remain in the reported hotspot list after the change | Compare the reported hotspot sets directly instead of assuming success from the microbench | Confirmed. Neither hotspot appears in the kept trace’s reported top exclusive list | Confirmed |

## 2026-03-12 typed opaque-mesher staging

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The current async opaque mesher is still paying too much JS staging overhead from dynamic `number[]` quad storage and `DataView`-driven vertex writes | Replace those with reusable typed quad scratch storage plus typed `Float32Array` / `Uint32Array` vertex writes, then compare both standalone mesher microbenches and the live 10-second walk benchmark | Confirmed. Dense and procedural mesher microbenches both improved, and the live walk stayed hole-free while moving from about `1.290 ms` to `1.265 ms` average gameplay frame | Confirmed |
| Worker-input cloning is still the dominant cost on the meshing path, so mesher-internal staging changes are probably a distraction | Time `createOpaqueChunkMeshingInput(..., { cloneData: true })` and `buildOpaqueChunkMeshFromInput(...)` separately on representative worlds before editing anything else | Rejected. Input cloning was tiny compared to actual opaque meshing work, so the better target was the mesher itself | Rejected |
| A mesher-only staging rewrite could silently change rendered output even if tests still pass | Re-run the worker-friendly opaque-mesher equivalence test and keep the slice only if the real browser route stays at `0` hole-signal frames | Confirmed. The equivalence test stayed green and the walk/trace runs both stayed at `0` hole-signal frames | Confirmed |
| A small mesher staging win in microbenches may disappear entirely in the real browser route | Compare the previous committed walk benchmark and live-forward trace directly instead of trusting only the standalone mesher timings | Confirmed. The walk benchmark and `avg_deltaTaskDurationMs` both improved, so the slice is worth keeping | Confirmed |

## 2026-03-12 neighbor-face-only async meshing inputs

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The async meshing worker still pays too much cross-thread overhead because it receives six full neighboring chunks even though it only samples their boundary faces | Replace neighbor chunk payloads in `OpaqueChunkMeshingInput` with extracted face planes, then compare the 10-second walk benchmark and live-forward Chrome trace against the current typed-mesher baseline | Confirmed. Walk average gameplay frame improved from about `1.265 ms` to `1.206 ms`, and the trace average improved from about `1.370 ms` to `1.319 ms` | Confirmed |
| The worker might still need full neighbor chunks for occlusion checks, so shrinking the payload could break hidden-face or fully-occluded-solid logic | Keep the synchronous mesher untouched, update only the async meshing input path, and rerun the worker-equivalence plus mesher focused tests | Confirmed. Focused tests stayed green and hole-signal frames remained `0` | Confirmed |
| `postMessage @ :` being high in the trace might be incidental noise rather than real transfer cost | Compare the reported trace hotspot sets directly after the payload shrink, not just average frame numbers | Confirmed. `postMessage @ :` exclusive dropped from about `12316.4 ms` to about `193.1 ms`, so the transfer payload really was the problem | Confirmed |
| The next async meshing win is probably another transfer-path cleanup after this slice | Re-read the new trace before assuming that | Rejected. Once the neighbor payload shrank, the dominant remaining cost stayed inside the chunk-meshing worker itself, so the next pass should target mesher-internal work rather than another large boundary-payload rewrite | Rejected |

## 2026-03-13 rejected inline render-summary generation

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The post-generation `summarizeGeneratedChunkRender(...)` pass is still enough wasted work that inlining render-summary construction into `generateChunk(...)` will improve real gameplay, not just generator microbenches | Build the summary inline, then compare the generator batch microbench, 10-second walk benchmark, and live-forward Chrome trace before keeping the extra scratch state | Rejected. The generator microbench improved slightly, but the walk benchmark and live-forward trace both regressed, so the whole slice was reverted | Rejected |
| The extra render-summary scratch arrays and helper functions are acceptable complexity if the generator microbench moves the right way | Keep the change only if the browser gates also move the right way | Rejected. This was a local optimum, not a player-path win | Rejected |

## 2026-03-13 fully solid opaque chunk fast path

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| A large share of streamed chunks are fully opaque filled volumes, and those chunks should bypass the full 3D greedy sweep in the async opaque mesher | Sample representative gameplay chunks first, then add a fully-solid face-only fast path and compare a targeted full-solid mesher microbench plus the browser walk/trace gates | Confirmed. About `26.1%` of sampled chunks were fully filled and fully opaque, the targeted mesher microbench dropped from about `208.448 ms` to `37.774 ms`, and the browser walk/trace metrics improved | Confirmed |
| A face-only fast path for filled chunks is too specialized and will become invalid once edits create stranger worlds | Keep it generic by keying only off actual chunk data plus real neighbor face snapshots, then rerun worker-equivalence tests on a fully solid chunk with partial neighbor-face exposure | Confirmed. The new focused test stayed green, so the fast path remains valid for edited worlds as long as the chunk is actually fully opaque-filled | Confirmed |
| The remaining meshing hotspot is still probably transfer or queue overhead, not mesher-internal work | Re-read the trace and compare it against the new fast-path browser runs instead of assuming that | Rejected. The route still improves when mesher internals get cheaper, so the next likely win remains inside worker meshing math/data flow rather than at the transfer boundary | Rejected |

## 2026-03-13 specialized worker mask fill by axis

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| The async opaque mesher is still paying too much CPU in its generic mask-fill inner loops because every sampled voxel goes through helper calls and generic axis-coordinate indirection | Replace only the mask-fill stage with axis-specialized direct indexing, then compare a representative procedural terrain mesher microbench plus the 10-second walk benchmark and clean live-forward traces | Confirmed. The procedural terrain mesher microbench dropped from about `130.372 ms` to `44.410 ms`, and the 10-second walk benchmark improved from about `1.253 ms` to `1.198 ms` average gameplay frame | Confirmed |
| The specialization might accidentally change output ordering or geometry even if the worker path stays “visually plausible” | Keep the worker-equivalence tests exact and reject the slice if they stop matching the synchronous mesh byte-for-byte | Confirmed. The existing exact-output worker tests stayed green | Confirmed |
| The apparent win might only be a microbench illusion if the profiled route trace moves the wrong way | Re-run clean live-forward traces instead of trusting the first parallel trace | Mixed. The clean traces stayed hole-free and lowered mesh backlog, but average traced frame time was roughly flat-to-slightly worse; I kept the slice because the non-profiled walk benchmark improved clearly | Mixed |

## 2026-03-13 incremental render-ready column tracking

| Hypothesis | Tiny verification case | Result | Status |
| --- | --- | --- | --- |
| `buildRenderReadyColumnKeys()` is wasting main-thread time rebuilding a set that can be maintained incrementally from adoption/eviction/mesh-complete events | Replace the full rebuild with resident/render-ready column counts and a maintained ready-column set, then compare a direct mask-query microprobe plus the walk benchmark and clean route trace | Confirmed. The mask-query microprobe dropped from about `0.240 ms` to `0.097 ms`, the walk benchmark improved from about `1.198 ms` to `1.190 ms`, and the clean route trace improved from about `1.320 ms` to `1.302 ms` average gameplay frame | Confirmed |
| Incremental ready-column bookkeeping is too fragile because the world has too many mesh completion paths to keep in sync | Wire it through both `rebuildDirtyMeshes()` and the game controller’s async/sync mesh completion paths, and reject it if the existing render-ready mask regression breaks | Confirmed. The render-ready far-field mask regression stayed green, so the state seam is sound enough to keep | Confirmed |
| Even if the mask query itself gets cheaper, the change may not matter in real gameplay | Re-run the real 10-second forward-walk benchmark and clean live-forward trace, not just the microprobe | Confirmed. Both browser gates improved and hole-signal frames stayed at `0` | Confirmed |
